/******************************************************************************
*              (c), Copyright, MOTOROLA  INC - ECC                  *
*                                                                             *
* THIS CODE CONTAINS CONFIDENTIAL INFORMATION OF MOTOROLA  INC - ECC, INC.        *
* NO RIGHTS ARE GRANTED HEREIN UNDER ANY PATENT, MASK WORK RIGHT OR COPYRIGHT *
* OF MOTOROLA   OR ANY THIRD PARTY. MOTOROLA   RESERVES THE RIGHT AT ITS SOLE DISCRETION*
* TO REQUEST THAT THIS CODE BE IMMEDIATELY RETURNED TO MOTOROLA  INC - ECC.       *
* THIS CODE IS PROVIDED "AS IS". MOTOROLA  INC - ECC MAKES NO WARRANTIES, EXPRESS,*
* IMPLIED OR OTHERWISE, REGARDING ITS ACCURACY, COMPLETENESS OR PERFORMANCE.  *
*******************************************************************************
*                                                                             *
* Filename:Dump.c                                                             *
*                                                                             *
* DESCRIPTION:	                                                              *
*       This file is generated by Regparse tool.                              *
*                                                                             *
* DEPENDENCIES:	                                                              *
*       None.                                                                 *
*                                                                             *
******************************************************************************/

#ifndef LONG_ACC
#define	LONG_ACC(v)           (v)
#endif
#ifndef WORD_ACC
#define	WORD_ACC(v)           (v)
#endif

#include <vxWorks.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Structs.h"
#include "Regs.h"
#include "Bits.h"

#include "./../../coreDrv/gtPci.h"

void
DCD_regs_dump ( regs, p, filter)
    DCD_REGS	*regs;
    char	*p;
    char	*filter;
{
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("BASE", filter) != NULL))
	{
	reg = regs->base; reg = LONG_ACC(reg);
	printf("%sRegister: BASE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->base, reg);
	printf ("\n%s         ", p);
	printf (" ADRS[0:19]=%lx", ((reg & 0xfffff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SIZE", filter) != NULL))
	{
	reg = regs->size; reg = LONG_ACC(reg);
	printf("%sRegister: SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->size, reg);
	printf ("\n%s         ", p);
	printf (" ADRS[0:15]=%lx", ((reg & 0xffff)>>0));
	printf ("\n");

	}

    free (np);
}		/* end of DCD_regs_dump()*/


void
PCI_DCD_regs_dump( regs, p, filter)
    PCI_DCD_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("BASE", filter) != NULL))
	{
		 char *bnames[1][4]=
		{{"b", "n", "bw", "w"},
		 };
	reg = regs->base; reg = LONG_ACC(reg);
	printf("%sRegister: BASE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->base, reg);
	printf ("\n%s         ", p);
	printf (" ADRS[0:19]=%lx", ((reg & 0xfffff)>>0));
	printf (" SWAP[24:25]=%s", bnames[0][((reg & 0x3000000)>>24)]);
	printf (" NS[26]=%lx", ((reg & 0x4000000)>>26));
	printf (" PCIReq64[27]=%lx", ((reg & 0x8000000)>>27));
	printf ("\n");

	}

    free (np);
}		/* end of PCI_DCD_regs_dump()*/


void
PCI_REMAP_regs_dump( regs, p, filter)
    PCI_REMAP_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("REMAP", filter) != NULL))
	{
	reg = regs->remap; reg = LONG_ACC(reg);
	printf("%sRegister: REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->remap, reg);
	printf ("\n%s         ", p);
	printf (" ADRS[0:15]=%lx", ((reg & 0xffff)>>0));
	printf ("\n");

	}

    free (np);
}		/* end of PCI_REMAP_regs_dump()*/


void
mpsc_cause_regs_dump( regs, p, filter)
    MPSC_CAUSE_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("x", filter) != NULL))
	{
	reg = regs->x; reg = LONG_ACC(reg);
	printf("%sRegister: x  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->x, reg);
	printf ("\n%s         ", p);
	printf (" rc[0]=%lx", ((reg & 0x1)>>0));
	printf (" rxerr[1]=%lx", ((reg & 0x2)>>1));
	printf (" txerr[3]=%lx", ((reg & 0x8)>>3));
	printf (" rlsc[4]=%lx", ((reg & 0x10)>>4));
	printf ("\n%s         ", p);
	printf (" rhnt[5]=%lx", ((reg & 0x20)>>5));
	printf (" rfsc[6]=%lx", ((reg & 0x40)>>6));
	printf (" rcsc[7]=%lx", ((reg & 0x80)>>7));
	printf (" rovr[8]=%lx", ((reg & 0x100)>>8));
	printf ("\n%s         ", p);
	printf (" rcdl[9]=%lx", ((reg & 0x200)>>9));
	printf (" rckg[10]=%lx", ((reg & 0x400)>>10));
	printf (" bper[11]=%lx", ((reg & 0x800)>>11));
	printf (" teidl[12]=%lx", ((reg & 0x1000)>>12));
	printf ("\n%s         ", p);
	printf (" tudr[13]=%lx", ((reg & 0x2000)>>13));
	printf (" tctsl[14]=%lx", ((reg & 0x4000)>>14));
	printf (" tckg[15]=%lx", ((reg & 0x8000)>>15));
	printf ("\n");

	}

    free (np);
}		/* end of mpsc_cause_regs_dump()*/


void
brg_regs_dump( regs, p, filter)
    BRG_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("cfg", filter) != NULL))
	{
		 char *bnames[1][16]=
		{{"bclkin", "1", "sclk0", "tsclk0", "4", "5", "sclk1", "tsclk1", "tclk", "9", "a", "b", "c", "d", "e", "f"},
		 };
	reg = regs->cfg; reg = LONG_ACC(reg);
	printf("%sRegister: cfg  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cfg, reg);
	printf ("\n%s         ", p);
	printf (" cdv[0:15]=%lx", ((reg & 0xffff)>>0));
	printf (" en[16]=%lx", ((reg & 0x10000)>>16));
	printf (" rst[17]=%lx", ((reg & 0x20000)>>17));
	printf (" clks[18:21]=%s", bnames[0][((reg & 0x3c0000)>>18)]);
	printf ("\n%s         ", p);
	printf (" bten[25]=%lx", ((reg & 0x2000000)>>25));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("tune", filter) != NULL))
	{
	reg = regs->tune; reg = LONG_ACC(reg);
	printf("%sRegister: tune  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->tune, reg);
	printf ("\n%s         ", p);
	printf (" cuv[0:15]=%lx", ((reg & 0xffff)>>0));
	printf ("\n");

	}

    free (np);
}		/* end of brg_regs_dump()*/


void
mpsc_regs_dump( regs, p, filter)
    MPSC_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("BASE", filter) != NULL))
	{
	printf("%sRegister: BASE  Address: 0x%08x  Bytes: 0",
		 p, (int)&regs->base[0]);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MAIN_CFG_LOW", filter) != NULL))
	{
		 char *bnames[3][8]=
		{{"hdlc", "1", "2", "3", "uart", "bisync", "6", "7"},
		 {"n", "lpb", "echo", "lpb_echo"},
		 {"crc16_c", "crc16", "crc32_c", "3", "4", "5", "6", "7"},
		 };
	reg = regs->mainCfgLow; reg = LONG_ACC(reg);
	printf("%sRegister: MAIN_CFG_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mainCfgLow, reg);
	printf ("\n%s         ", p);
	printf (" mode[0:2]=%s", bnames[0][((reg & 0x7)>>0)]);
	printf (" ttx[3]=%lx", ((reg & 0x8)>>3));
	printf (" trx[4]=%lx", ((reg & 0x10)>>4));
	printf (" et[6]=%lx", ((reg & 0x40)>>6));
	printf ("\n%s         ", p);
	printf (" er[7]=%lx", ((reg & 0x80)>>7));
	printf (" lpbk[8:9]=%s", bnames[1][((reg & 0x300)>>8)]);
	printf (" nlm[10]=%lx", ((reg & 0x400)>>10));
	printf (" tsyn[12]=%lx", ((reg & 0x1000)>>12));
	printf ("\n%s         ", p);
	printf (" tsns[14:15]=%lx", ((reg & 0xc000)>>14));
	printf (" tidl[16]=%lx", ((reg & 0x10000)>>16));
	printf (" rtsm[17]=%lx", ((reg & 0x20000)>>17));
	printf (" ctss[19]=%lx", ((reg & 0x80000)>>19));
	printf ("\n%s         ", p);
	printf (" cds[20]=%lx", ((reg & 0x100000)>>20));
	printf (" ctsm[21]=%lx", ((reg & 0x200000)>>21));
	printf (" cdm[22]=%lx", ((reg & 0x400000)>>22));
	printf (" crcm[23:25]=%s", bnames[2][((reg & 0x3800000)>>23)]);
	printf ("\n%s         ", p);
	printf (" trvd[28]=%lx", ((reg & 0x10000000)>>28));
	printf (" rrvd[29]=%lx", ((reg & 0x20000000)>>29));
	printf (" gde[31]=%lx", ((reg & 0x80000000)>>31));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MAIN_CFG_HI", filter) != NULL))
	{
	reg = regs->mainCfgHi; reg = LONG_ACC(reg);
	printf("%sRegister: MAIN_CFG_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mainCfgHi, reg);
	printf ("\n%s         ", p);
	printf (" tci[0]=%lx", ((reg & 0x1)>>0));
	printf (" tinv[1]=%lx", ((reg & 0x2)>>1));
	printf (" tpl[2:4]=%lx", ((reg & 0x1c)>>2));
	printf (" tppt[5:8]=%lx", ((reg & 0x1e0)>>5));
	printf ("\n%s         ", p);
	printf (" tcdv[9:10]=%lx", ((reg & 0x600)>>9));
	printf (" tdec[11:13]=%lx", ((reg & 0x3800)>>11));
	printf (" rinv[16]=%lx", ((reg & 0x10000)>>16));
	printf (" gdw[17:20]=%lx", ((reg & 0x1e0000)>>17));
	printf ("\n%s         ", p);
	printf (" rdw[22]=%lx", ((reg & 0x400000)>>22));
	printf (" rsyl[23:24]=%lx", ((reg & 0x1800000)>>23));
	printf (" rcdv[25:26]=%lx", ((reg & 0x6000000)>>25));
	printf (" renc[27:29]=%lx", ((reg & 0x38000000)>>27));
	printf ("\n%s         ", p);
	printf (" sedg[30:31]=%lx", ((reg & 0xc0000000)>>30));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PROTO_CFG", filter) != NULL))
	{
		 char *bnames[1][4]=
		{{"norm", "mdm", "2", "3"},
		 };
	reg = regs->protoCfg; reg = LONG_ACC(reg);
	printf("%sRegister: PROTO_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->protoCfg, reg);
	printf ("\n%s         ", p);
	printf (" u_drt[6]=%lx", ((reg & 0x40)>>6));
	printf (" u_iso[7]=%lx", ((reg & 0x80)>>7));
	printf (" u_rs[8]=%lx", ((reg & 0x100)>>8));
	printf (" u_frz[9]=%lx", ((reg & 0x200)>>9));
	printf ("\n%s         ", p);
	printf (" u_um[10:11]=%s", bnames[0][((reg & 0xc00)>>10)]);
	printf (" u_cl[12:13]=%lx", ((reg & 0x3000)>>12));
	printf (" u_sbl[14]=%lx", ((reg & 0x4000)>>14));
	printf (" u_flc[15]=%lx", ((reg & 0x8000)>>15));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG1", filter) != NULL))
	{
	reg = regs->reg1; reg = LONG_ACC(reg);
	printf("%sRegister: REG1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg1, reg);
	printf ("\n%s         ", p);
	printf (" u_tcs[0:7]=%lx", ((reg & 0xff)>>0));
	printf (" u_brk[16:23]=%lx", ((reg & 0xff0000)>>16));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG2", filter) != NULL))
	{
	reg = regs->reg2; reg = LONG_ACC(reg);
	printf("%sRegister: REG2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg2, reg);
	printf ("\n%s         ", p);
	printf (" u_tev[1]=%lx", ((reg & 0x2)>>1));
	printf (" u_tpm[2:3]=%lx", ((reg & 0xc)>>2));
	printf (" u_ra[7]=%lx", ((reg & 0x80)>>7));
	printf (" u_ttcs[9]=%lx", ((reg & 0x200)>>9));
	printf ("\n%s         ", p);
	printf (" u_rev[17]=%lx", ((reg & 0x20000)>>17));
	printf (" u_rpm[18:19]=%lx", ((reg & 0xc0000)>>18));
	printf (" u_ta[23]=%lx", ((reg & 0x800000)>>23));
	printf (" u_crd[25]=%lx", ((reg & 0x2000000)>>25));
	printf ("\n%s         ", p);
	printf (" u_eh[31]=%lx", ((reg & 0x80000000)>>31));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG3", filter) != NULL))
	{
	reg = regs->reg3; reg = LONG_ACC(reg);
	printf("%sRegister: REG3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg3, reg);
	printf ("\n%s         ", p);
	printf (" u_mir[0:15]=%lx", ((reg & 0xffff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG4", filter) != NULL))
	{
	reg = regs->reg4; reg = LONG_ACC(reg);
	printf("%sRegister: REG4  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg4, reg);
	printf ("\n%s         ", p);
	printf (" cfr_bce[0:7]=%lx", ((reg & 0xff)>>0));
	printf (" cfr_z[29]=%lx", ((reg & 0x20000000)>>29));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG5", filter) != NULL))
	{
	reg = regs->reg5; reg = LONG_ACC(reg);
	printf("%sRegister: REG5  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg5, reg);
	printf ("\n%s         ", p);
	printf (" u_chr[0:7]=%lx", ((reg & 0xff)>>0));
	printf (" u_int[12]=%lx", ((reg & 0x1000)>>12));
	printf (" u_co[13]=%lx", ((reg & 0x2000)>>13));
	printf (" u_r[14]=%lx", ((reg & 0x4000)>>14));
	printf ("\n%s         ", p);
	printf (" u_v[15]=%lx", ((reg & 0x8000)>>15));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG6", filter) != NULL))
	{
	reg = regs->reg6; reg = LONG_ACC(reg);
	printf("%sRegister: REG6  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg6, reg);
	printf ("\n%s         ", p);
	printf (" u_chr[0:7]=%lx", ((reg & 0xff)>>0));
	printf (" u_int[12]=%lx", ((reg & 0x1000)>>12));
	printf (" u_co[13]=%lx", ((reg & 0x2000)>>13));
	printf (" u_r[14]=%lx", ((reg & 0x4000)>>14));
	printf ("\n%s         ", p);
	printf (" u_v[15]=%lx", ((reg & 0x8000)>>15));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG7", filter) != NULL))
	{
	reg = regs->reg7; reg = LONG_ACC(reg);
	printf("%sRegister: REG7  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg7, reg);
	printf ("\n%s         ", p);
	printf (" u_chr[0:7]=%lx", ((reg & 0xff)>>0));
	printf (" u_int[12]=%lx", ((reg & 0x1000)>>12));
	printf (" u_co[13]=%lx", ((reg & 0x2000)>>13));
	printf (" u_r[14]=%lx", ((reg & 0x4000)>>14));
	printf ("\n%s         ", p);
	printf (" u_v[15]=%lx", ((reg & 0x8000)>>15));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG8", filter) != NULL))
	{
	reg = regs->reg8; reg = LONG_ACC(reg);
	printf("%sRegister: REG8  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg8, reg);
	printf ("\n%s         ", p);
	printf (" u_chr[0:7]=%lx", ((reg & 0xff)>>0));
	printf (" u_int[12]=%lx", ((reg & 0x1000)>>12));
	printf (" u_co[13]=%lx", ((reg & 0x2000)>>13));
	printf (" u_r[14]=%lx", ((reg & 0x4000)>>14));
	printf ("\n%s         ", p);
	printf (" u_v[15]=%lx", ((reg & 0x8000)>>15));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG9", filter) != NULL))
	{
	reg = regs->reg9; reg = LONG_ACC(reg);
	printf("%sRegister: REG9  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg9, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("REG10", filter) != NULL))
	{
	reg = regs->reg10; reg = LONG_ACC(reg);
	printf("%sRegister: REG10  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->reg10, reg);
	printf ("\n%s         ", p);
	printf (" u_cts[0]=%lx", ((reg & 0x1)>>0));
	printf (" u_cd[1]=%lx", ((reg & 0x2)>>1));
	printf (" u_tidle[3]=%lx", ((reg & 0x8)>>3));
	printf (" u_rhs[5]=%lx", ((reg & 0x20)>>5));
	printf ("\n%s         ", p);
	printf (" u_rls[7]=%lx", ((reg & 0x80)>>7));
	printf (" u_rlidl[11]=%lx", ((reg & 0x800)>>11));
	printf (" u_rcrn[16:23]=%lx", ((reg & 0xff0000)>>16));
	printf ("\n");

	}

    free (np);
}		/* end of mpsc_regs_dump()*/


void
SDMA_RXBD_regs_dump( regs, p, filter)
    SDMA_RXBD_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("size", filter) != NULL))
	{
	reg = regs->size; reg = WORD_ACC(reg);
	printf("%sRegister: size  Address: 0x%08x  Value: %04lx",
		 p, (int)&regs->size, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("bcount", filter) != NULL))
	{
	reg = regs->bcount; reg = WORD_ACC(reg);
	printf("%sRegister: bcount  Address: 0x%08x  Value: %04lx",
		 p, (int)&regs->bcount, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("cs", filter) != NULL))
	{
	reg = regs->cs; reg = LONG_ACC(reg);
	printf("%sRegister: cs  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs, reg);
	printf ("\n%s         ", p);
	printf (" L[16]=%lx", ((reg & 0x10000)>>16));
	printf (" F[17]=%lx", ((reg & 0x20000)>>17));
	printf (" EI[23]=%lx", ((reg & 0x800000)>>23));
	printf (" AM[30]=%lx", ((reg & 0x40000000)>>30));
	printf ("\n%s         ", p);
	printf (" O[31]=%lx", ((reg & 0x80000000)>>31));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("next", filter) != NULL))
	{
	reg = regs->next; reg = LONG_ACC(reg);
	printf("%sRegister: next  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->next, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("bptr", filter) != NULL))
	{
	reg = regs->bptr; reg = LONG_ACC(reg);
	printf("%sRegister: bptr  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->bptr, reg);
	printf ("\n");

	}

    free (np);
}		/* end of SDMA_RXBD_regs_dump()*/


void
SDMA_TXBD_regs_dump( regs, p, filter)
    SDMA_TXBD_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("bcount", filter) != NULL))
	{
	reg = regs->bcount; reg = WORD_ACC(reg);
	printf("%sRegister: bcount  Address: 0x%08x  Value: %04lx",
		 p, (int)&regs->bcount, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("shadow", filter) != NULL))
	{
	reg = regs->shadow; reg = WORD_ACC(reg);
	printf("%sRegister: shadow  Address: 0x%08x  Value: %04lx",
		 p, (int)&regs->shadow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("cs", filter) != NULL))
	{
	reg = regs->cs; reg = LONG_ACC(reg);
	printf("%sRegister: cs  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs, reg);
	printf ("\n%s         ", p);
	printf (" L[16]=%lx", ((reg & 0x10000)>>16));
	printf (" F[17]=%lx", ((reg & 0x20000)>>17));
	printf (" EI[23]=%lx", ((reg & 0x800000)>>23));
	printf (" AM[30]=%lx", ((reg & 0x40000000)>>30));
	printf ("\n%s         ", p);
	printf (" O[31]=%lx", ((reg & 0x80000000)>>31));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("next", filter) != NULL))
	{
	reg = regs->next; reg = LONG_ACC(reg);
	printf("%sRegister: next  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->next, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("bptr", filter) != NULL))
	{
	reg = regs->bptr; reg = LONG_ACC(reg);
	printf("%sRegister: bptr  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->bptr, reg);
	printf ("\n");

	}

    free (np);
}		/* end of SDMA_TXBD_regs_dump()*/


void
MV64360_regs_dump( regs, p, filter)
    MV64360_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("CPU_CONFIGURATION", filter) != NULL))
	{
	reg = regs->cpuConfiguration; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_CONFIGURATION  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuConfiguration, reg);
	printf ("\n%s         ", p);
	printf (" nm_cnt[0:7]=%lx", ((reg & 0xff)>>0));
	printf (" nm_cnt_en[8]=%lx", ((reg & 0x100)>>8));
	printf (" nm_cnt_ext[9]=%lx", ((reg & 0x200)>>9));
	printf (" sigle_cpu[11]=%lx", ((reg & 0x800)>>11));
	printf ("\n%s         ", p);
	printf (" end[12]=%lx", ((reg & 0x1000)>>12));
	printf (" pipe[13]=%lx", ((reg & 0x2000)>>13));
	printf (" stop_retry[17]=%lx", ((reg & 0x20000)>>17));
	printf (" multi_gt_dec[18]=%lx", ((reg & 0x40000)>>18));
	printf ("\n%s         ", p);
	printf (" dp_valid[19]=%lx", ((reg & 0x80000)>>19));
	printf (" perr_prop[22]=%lx", ((reg & 0x400000)>>22));
	printf (" aack_delay2[25]=%lx", ((reg & 0x2000000)>>25));
	printf (" apvalid[26]=%lx", ((reg & 0x4000000)>>26));
	printf ("\n%s         ", p);
	printf (" remap_wr_dis[27]=%lx", ((reg & 0x8000000)>>27));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("LOW_IRQ_CAUSE", filter) != NULL))
	{
	reg = regs->lowIrqCause; reg = LONG_ACC(reg);
	printf("%sRegister: LOW_IRQ_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->lowIrqCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CS0_DECODE", filter) != NULL))
	{
	reg = regs->cs0Decode; reg = LONG_ACC(reg);
	printf("%sRegister: CS0_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs0Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("HI_IRQ_CAUSE", filter) != NULL))
	{
	reg = regs->hiIrqCause; reg = LONG_ACC(reg);
	printf("%sRegister: HI_IRQ_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->hiIrqCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CS0_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->cs0DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: CS0_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs0DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_0_IRQ_MASK_LOW", filter) != NULL))
	{
	reg = regs->cpu0IrqMaskLow; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_0_IRQ_MASK_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu0IrqMaskLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CS2_DECODE", filter) != NULL))
	{
	reg = regs->cs2Decode; reg = LONG_ACC(reg);
	printf("%sRegister: CS2_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs2Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_0_IRQ_MASK_HI", filter) != NULL))
	{
	reg = regs->cpu0IrqMaskHi; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_0_IRQ_MASK_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu0IrqMaskHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CS2_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->cs2DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: CS2_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs2DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_0_SELECT_CAUSE", filter) != NULL))
	{
	reg = regs->cpu0SelectCause; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_0_SELECT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu0SelectCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DCS_0_DECODE", filter) != NULL))
	{
	reg = regs->dcs0Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DCS_0_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dcs0Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DCS_0_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->dcs0DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: DCS_0_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dcs0DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_1_IRQ_MASK_LOW", filter) != NULL))
	{
	reg = regs->cpu1IrqMaskLow; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_1_IRQ_MASK_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu1IrqMaskLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DCS_3_DECODE", filter) != NULL))
	{
	reg = regs->dcs3Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DCS_3_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dcs3Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_1_IRQ_MASK_HI", filter) != NULL))
	{
	reg = regs->cpu1IrqMaskHi; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_1_IRQ_MASK_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu1IrqMaskHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DCS_3_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->dcs3DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: DCS_3_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dcs3DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_1_SELECT_CAUSE", filter) != NULL))
	{
	reg = regs->cpu1SelectCause; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_1_SELECT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu1SelectCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_IO_DECODE", filter) != NULL))
	{
		 char *bnames[1][4]=
		{{"b", "n", "bw", "w"},
		 };
	reg = regs->pci0IoDecode; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_IO_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0IoDecode, reg);
	printf ("\n%s         ", p);
	printf (" ADRS[0:19]=%lx", ((reg & 0xfffff)>>0));
	printf (" SWAP[24:25]=%s", bnames[0][((reg & 0x3000000)>>24)]);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_IO_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci0IoDecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_IO_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0IoDecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INT0_IRQ_CAUSE_MASK_LOW", filter) != NULL))
	{
	reg = regs->int0IrqCauseMaskLow; reg = LONG_ACC(reg);
	printf("%sRegister: INT0_IRQ_CAUSE_MASK_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->int0IrqCauseMaskLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM0_DECODE", filter) != NULL))
	{
	printf ("%sPCI0_MEM0_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci0Mem0Decode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci0Mem0Decode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("INT0_IRQ_CAUSE_MASK_HI", filter) != NULL))
	{
	reg = regs->int0IrqCauseMaskHi; reg = LONG_ACC(reg);
	printf("%sRegister: INT0_IRQ_CAUSE_MASK_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->int0IrqCauseMaskHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM0_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci0Mem0DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MEM0_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mem0DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INT0_SELECT_CAUSE", filter) != NULL))
	{
	reg = regs->int0SelectCause; reg = LONG_ACC(reg);
	printf("%sRegister: INT0_SELECT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->int0SelectCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INTERNAL_SPACE_BASE", filter) != NULL))
	{
	reg = regs->internalSpaceBase; reg = LONG_ACC(reg);
	printf("%sRegister: INTERNAL_SPACE_BASE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->internalSpaceBase, reg);
	printf ("\n%s         ", p);
	printf (" base[0:19]=%lx", ((reg & 0xfffff)>>0));
	printf (" swap[24:25]=%lx", ((reg & 0x3000000)>>24));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_ERROR_ADRS_LOW", filter) != NULL))
	{
	reg = regs->cpuErrorAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_ERROR_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuErrorAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INT1_IRQ_CAUSE_MASK_LOW", filter) != NULL))
	{
	reg = regs->int1IrqCauseMaskLow; reg = LONG_ACC(reg);
	printf("%sRegister: INT1_IRQ_CAUSE_MASK_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->int1IrqCauseMaskLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_ERROR_ADRS_HI", filter) != NULL))
	{
	reg = regs->cpuErrorAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_ERROR_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuErrorAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INT1_IRQ_CAUSE_MASK_HI", filter) != NULL))
	{
	reg = regs->int1IrqCauseMaskHi; reg = LONG_ACC(reg);
	printf("%sRegister: INT1_IRQ_CAUSE_MASK_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->int1IrqCauseMaskHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM1_DECODE", filter) != NULL))
	{
	printf ("%sPCI0_MEM1_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci0Mem1Decode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci0Mem1Decode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("INT1_SELECT_CAUSE", filter) != NULL))
	{
	reg = regs->int1SelectCause; reg = LONG_ACC(reg);
	printf("%sRegister: INT1_SELECT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->int1SelectCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM1_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci0Mem1DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MEM1_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mem1DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_IO_DECODE", filter) != NULL))
	{
		/* char *bnames[1][4]=
		{{"b", "n", "bw", "w"},
		 };*/ /*vijay*/

	printf ("%sPCI1_IO_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci1IoDecode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci1IoDecode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_IO_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci1IoDecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_IO_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1IoDecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MEM0_DECODE", filter) != NULL))
	{
	printf ("%sPCI1_MEM0_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci1Mem0Decode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci1Mem0Decode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_MEM0_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci1Mem0DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MEM0_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mem0DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MEM1_DECODE", filter) != NULL))
	{
	printf ("%sPCI1_MEM1_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci1Mem1Decode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci1Mem1Decode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_MEM1_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci1Mem1DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MEM1_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mem1DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU0_SYNC_BARIER_TRIGGER", filter) != NULL))
	{
	reg = regs->cpu0SyncBarierTrigger; reg = LONG_ACC(reg);
	printf("%sRegister: CPU0_SYNC_BARIER_TRIGGER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu0SyncBarierTrigger, reg);
	printf ("\n%s         ", p);
	printf (" sbtrig[0:3]=%lx", ((reg & 0xf)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU0_SYNC_BARIER_VIRTUAL", filter) != NULL))
	{
	reg = regs->cpu0SyncBarierVirtual; reg = LONG_ACC(reg);
	printf("%sRegister: CPU0_SYNC_BARIER_VIRTUAL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu0SyncBarierVirtual, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU1_SYNC_BARIER_TRIGGER", filter) != NULL))
	{
	reg = regs->cpu1SyncBarierTrigger; reg = LONG_ACC(reg);
	printf("%sRegister: CPU1_SYNC_BARIER_TRIGGER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu1SyncBarierTrigger, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU1_SYNC_BARIER_VIRTUAL", filter) != NULL))
	{
	reg = regs->cpu1SyncBarierVirtual; reg = LONG_ACC(reg);
	printf("%sRegister: CPU1_SYNC_BARIER_VIRTUAL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu1SyncBarierVirtual, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_IO_REMAP", filter) != NULL))
	{
	printf ("%sPCI0_IO_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci0IoRemap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci0IoRemap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI0_MEM0_REMAP", filter) != NULL))
	{
	printf ("%sPCI0_MEM0_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci0Mem0Remap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci0Mem0Remap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI0_MEM1_REMAP", filter) != NULL))
	{
	printf ("%sPCI0_MEM1_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci0Mem1Remap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci0Mem1Remap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_IO_REMAP", filter) != NULL))
	{
	printf ("%sPCI1_IO_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci1IoRemap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci1IoRemap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_MEM0_REMAP", filter) != NULL))
	{
	printf ("%sPCI1_MEM0_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci1Mem0Remap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci1Mem0Remap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_MEM1_REMAP", filter) != NULL))
	{
	printf ("%sPCI1_MEM1_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci1Mem1Remap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci1Mem1Remap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("CPU_MODE", filter) != NULL))
	{
	reg = regs->cpuMode; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_MODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuMode, reg);
	printf ("\n%s         ", p);
	printf (" multi_gt_id[0:1]=%lx", ((reg & 0x3)>>0));
	printf (" multi_gt[2]=%lx", ((reg & 0x4)>>2));
	printf (" retry_en[3]=%lx", ((reg & 0x8)>>3));
	printf (" cpu_type[4:7]=%lx", ((reg & 0xf0)>>4));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_ERROR_DATA_LOW", filter) != NULL))
	{
	reg = regs->cpuErrorDataLow; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_ERROR_DATA_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuErrorDataLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_ERROR_DATA_HI", filter) != NULL))
	{
	reg = regs->cpuErrorDataHi; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_ERROR_DATA_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuErrorDataHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_ERROR_PARITY", filter) != NULL))
	{
	reg = regs->cpuErrorParity; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_ERROR_PARITY  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuErrorParity, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_ERROR_CAUSE", filter) != NULL))
	{
	reg = regs->cpuErrorCause; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_ERROR_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuErrorCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_ERROR_MASK", filter) != NULL))
	{
	reg = regs->cpuErrorMask; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_ERROR_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuErrorMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_CROSS_BAR_CTRL_LOW", filter) != NULL))
	{
	reg = regs->cpuCrossBarCtrlLow; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_CROSS_BAR_CTRL_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuCrossBarCtrlLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_CROSS_BAR_CTRL_HIGH", filter) != NULL))
	{
	reg = regs->cpuCrossBarCtrlHigh; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_CROSS_BAR_CTRL_HIGH  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuCrossBarCtrlHigh, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_MASTER_CTRL", filter) != NULL))
	{
	reg = regs->cpuMasterCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_MASTER_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuMasterCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_CROSS_BAR_TIMEOUT", filter) != NULL))
	{
	reg = regs->cpuCrossBarTimeout; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_CROSS_BAR_TIMEOUT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuCrossBarTimeout, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_PROT_BASE_ADRS_0", filter) != NULL))
	{
	reg = regs->cpuProtBaseAdrs0; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_PROT_BASE_ADRS_0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuProtBaseAdrs0, reg);
	printf ("\n%s         ", p);
	printf (" base[0:19]=%lx", ((reg & 0xfffff)>>0));
	printf (" accprot[20]=%lx", ((reg & 0x100000)>>20));
	printf (" wrprot[21]=%lx", ((reg & 0x200000)>>21));
	printf (" cacheprot[22]=%lx", ((reg & 0x400000)>>22));
	printf ("\n%s         ", p);
	printf (" en[31]=%lx", ((reg & 0x80000000)>>31));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_PROT_SIZE_0", filter) != NULL))
	{
	reg = regs->cpuProtSize0; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_PROT_SIZE_0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuProtSize0, reg);
	printf ("\n%s         ", p);
	printf (" size[0:15]=%lx", ((reg & 0xffff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_PROT_BASE_ADRS_1", filter) != NULL))
	{
	reg = regs->cpuProtBaseAdrs1; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_PROT_BASE_ADRS_1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuProtBaseAdrs1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_PROT_SIZE_1", filter) != NULL))
	{
	reg = regs->cpuProtSize1; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_PROT_SIZE_1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuProtSize1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_PROT_BASE_ADRS_2", filter) != NULL))
	{
	reg = regs->cpuProtBaseAdrs2; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_PROT_BASE_ADRS_2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuProtBaseAdrs2, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_PROT_SIZE_2", filter) != NULL))
	{
	reg = regs->cpuProtSize2; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_PROT_SIZE_2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuProtSize2, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_PROT_BASE_ADRS_3", filter) != NULL))
	{
	reg = regs->cpuProtBaseAdrs3; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_PROT_BASE_ADRS_3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuProtBaseAdrs3, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_PROT_SIZE_3", filter) != NULL))
	{
	reg = regs->cpuProtSize3; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_PROT_SIZE_3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpuProtSize3, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("WHO_AM_I", filter) != NULL))
	{
	reg = regs->whoAmI; reg = LONG_ACC(reg);
	printf("%sRegister: WHO_AM_I  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->whoAmI, reg);
	printf ("\n%s         ", p);
	printf (" id[0:1]=%lx", ((reg & 0x3)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CS1_DECODE", filter) != NULL))
	{
	reg = regs->cs1Decode; reg = LONG_ACC(reg);
	printf("%sRegister: CS1_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs1Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CS1_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->cs1DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: CS1_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs1DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_0_DOOR_BELL", filter) != NULL))
	{
	reg = regs->cpu0DoorBell; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_0_DOOR_BELL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu0DoorBell, reg);
	printf ("\n%s         ", p);
	printf (" db[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CS3_DECODE", filter) != NULL))
	{
	reg = regs->cs3Decode; reg = LONG_ACC(reg);
	printf("%sRegister: CS3_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs3Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_0_DOOR_BELL_CLEAR", filter) != NULL))
	{
	reg = regs->cpu0DoorBellClear; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_0_DOOR_BELL_CLEAR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu0DoorBellClear, reg);
	printf ("\n%s         ", p);
	printf (" clear[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CS3_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->cs3DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: CS3_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cs3DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_1_DOOR_BELL", filter) != NULL))
	{
	reg = regs->cpu1DoorBell; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_1_DOOR_BELL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu1DoorBell, reg);
	printf ("\n%s         ", p);
	printf (" db[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DCS_1_DECODE", filter) != NULL))
	{
	reg = regs->dcs1Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DCS_1_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dcs1Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_1_DOOR_BELL_CLEAR", filter) != NULL))
	{
	reg = regs->cpu1DoorBellClear; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_1_DOOR_BELL_CLEAR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu1DoorBellClear, reg);
	printf ("\n%s         ", p);
	printf (" clear[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DCS_1_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->dcs1DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: DCS_1_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dcs1DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_0_DOOR_BELL_MASK", filter) != NULL))
	{
	reg = regs->cpu0DoorBellMask; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_0_DOOR_BELL_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu0DoorBellMask, reg);
	printf ("\n%s         ", p);
	printf (" mask[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("BOOTCS_DECODE", filter) != NULL))
	{
	reg = regs->bootcsDecode; reg = LONG_ACC(reg);
	printf("%sRegister: BOOTCS_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->bootcsDecode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CPU_1_DOOR_BELL_MASK", filter) != NULL))
	{
	reg = regs->cpu1DoorBellMask; reg = LONG_ACC(reg);
	printf("%sRegister: CPU_1_DOOR_BELL_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->cpu1DoorBellMask, reg);
	printf ("\n%s         ", p);
	printf (" mask[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("BOOTCS_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->bootcsDecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: BOOTCS_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->bootcsDecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SEMAPHORE_0", filter) != NULL))
	{
	reg = regs->semaphore0; reg = LONG_ACC(reg);
	printf("%sRegister: SEMAPHORE_0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->semaphore0, reg);
	printf ("\n%s         ", p);
	printf (" sem[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DCS_2_DECODE", filter) != NULL))
	{
	reg = regs->dcs2Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DCS_2_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dcs2Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SEMAPHORE_1", filter) != NULL))
	{
	reg = regs->semaphore1; reg = LONG_ACC(reg);
	printf("%sRegister: SEMAPHORE_1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->semaphore1, reg);
	printf ("\n%s         ", p);
	printf (" sem[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DCS_2_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->dcs2DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: DCS_2_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dcs2DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SEMAPHORE_2", filter) != NULL))
	{
	reg = regs->semaphore2; reg = LONG_ACC(reg);
	printf("%sRegister: SEMAPHORE_2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->semaphore2, reg);
	printf ("\n%s         ", p);
	printf (" sem[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM2_DECODE", filter) != NULL))
	{
	printf ("%sPCI0_MEM2_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci0Mem2Decode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci0Mem2Decode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("SEMAPHORE_3", filter) != NULL))
	{
	reg = regs->semaphore3; reg = LONG_ACC(reg);
	printf("%sRegister: SEMAPHORE_3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->semaphore3, reg);
	printf ("\n%s         ", p);
	printf (" sem[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM2_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci0Mem2DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MEM2_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mem2DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SEMAPHORE_4", filter) != NULL))
	{
	reg = regs->semaphore4; reg = LONG_ACC(reg);
	printf("%sRegister: SEMAPHORE_4  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->semaphore4, reg);
	printf ("\n%s         ", p);
	printf (" sem[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INTEGRATED_SRAM_BASE", filter) != NULL))
	{
	reg = regs->integratedSramBase; reg = LONG_ACC(reg);
	printf("%sRegister: INTEGRATED_SRAM_BASE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->integratedSramBase, reg);
	printf ("\n%s         ", p);
	printf (" ADRS[0:19]=%lx", ((reg & 0xfffff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SEMAPHORE_5", filter) != NULL))
	{
	reg = regs->semaphore5; reg = LONG_ACC(reg);
	printf("%sRegister: SEMAPHORE_5  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->semaphore5, reg);
	printf ("\n%s         ", p);
	printf (" sem[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SEMAPHORE_6", filter) != NULL))
	{
	reg = regs->semaphore6; reg = LONG_ACC(reg);
	printf("%sRegister: SEMAPHORE_6  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->semaphore6, reg);
	printf ("\n%s         ", p);
	printf (" sem[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("BASE_ADDR_ENABLE", filter) != NULL))
	{
	reg = regs->baseAddrEnable; reg = LONG_ACC(reg);
	printf("%sRegister: BASE_ADDR_ENABLE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->baseAddrEnable, reg);
	printf ("\n%s         ", p);
	printf (" en[0:20]=%lx", ((reg & 0x1fffff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SEMAPHORE_7", filter) != NULL))
	{
	reg = regs->semaphore7; reg = LONG_ACC(reg);
	printf("%sRegister: SEMAPHORE_7  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->semaphore7, reg);
	printf ("\n%s         ", p);
	printf (" sem[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM3_DECODE", filter) != NULL))
	{
	printf ("%sPCI0_MEM3_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci0Mem3Decode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci0Mem3Decode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI0_MEM3_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci0Mem3DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MEM3_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mem3DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MEM2_DECODE", filter) != NULL))
	{
	printf ("%sPCI1_MEM2_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci1Mem2Decode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci1Mem2Decode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_MEM2_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci1Mem2DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MEM2_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mem2DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MEM3_DECODE", filter) != NULL))
	{
	printf ("%sPCI1_MEM3_DECODE  (Address 0x%08x)\n", p, (int)&regs->pci1Mem3Decode);
	{
		sprintf (np, "  %s", p);
		PCI_DCD_regs_dump( &regs->pci1Mem3Decode, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_MEM3_DECODE_SIZE", filter) != NULL))
	{
	reg = regs->pci1Mem3DecodeSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MEM3_DECODE_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mem3DecodeSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM2_REMAP", filter) != NULL))
	{
	printf ("%sPCI0_MEM2_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci0Mem2Remap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci0Mem2Remap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI0_MEM3_REMAP", filter) != NULL))
	{
	printf ("%sPCI0_MEM3_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci0Mem3Remap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci0Mem3Remap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_MEM2_REMAP", filter) != NULL))
	{
	printf ("%sPCI1_MEM2_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci1Mem2Remap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci1Mem2Remap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI1_MEM3_REMAP", filter) != NULL))
	{
	printf ("%sPCI1_MEM3_REMAP  (Address 0x%08x)\n", p, (int)&regs->pci1Mem3Remap);
	{
		sprintf (np, "  %s", p);
		PCI_REMAP_regs_dump( &regs->pci1Mem3Remap, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("PCI0_MEM0_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci0Mem0RemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MEM0_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mem0RemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM1_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci0Mem1RemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MEM1_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mem1RemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM2_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci0Mem2RemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MEM2_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mem2RemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MEM3_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci0Mem3RemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MEM3_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mem3RemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MEM0_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci1Mem0RemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MEM0_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mem0RemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MEM1_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci1Mem1RemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MEM1_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mem1RemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MEM2_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci1Mem2RemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MEM2_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mem2RemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MEM3_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci1Mem3RemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MEM3_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mem3RemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SRAM_CFG", filter) != NULL))
	{
	reg = regs->sramCfg; reg = LONG_ACC(reg);
	printf("%sRegister: SRAM_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sramCfg, reg);
	printf ("\n%s         ", p);
	printf (" cc_en[0:1]=%lx", ((reg & 0x3)>>0));
	printf (" par_en[4]=%lx", ((reg & 0x10)>>4));
	printf (" perr_prop[5]=%lx", ((reg & 0x20)>>5));
	printf (" frc_par_en[6]=%lx", ((reg & 0x40)>>6));
	printf ("\n%s         ", p);
	printf (" park[7]=%lx", ((reg & 0x80)>>7));
	printf (" frc_par[8:15]=%lx", ((reg & 0xff00)>>8));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SRAM_ERR_CAUSE", filter) != NULL))
	{
	reg = regs->sramErrCause; reg = LONG_ACC(reg);
	printf("%sRegister: SRAM_ERR_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sramErrCause, reg);
	printf ("\n%s         ", p);
	printf (" err[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SRAM_ERR_ADRS", filter) != NULL))
	{
	reg = regs->sramErrAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: SRAM_ERR_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sramErrAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SRAM_ERR_DATA_LOW", filter) != NULL))
	{
	reg = regs->sramErrDataLow; reg = LONG_ACC(reg);
	printf("%sRegister: SRAM_ERR_DATA_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sramErrDataLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SRAM_ERR_DATA_HI", filter) != NULL))
	{
	reg = regs->sramErrDataHi; reg = LONG_ACC(reg);
	printf("%sRegister: SRAM_ERR_DATA_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sramErrDataHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SRAM_ERR_PARITY", filter) != NULL))
	{
	reg = regs->sramErrParity; reg = LONG_ACC(reg);
	printf("%sRegister: SRAM_ERR_PARITY  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sramErrParity, reg);
	printf ("\n%s         ", p);
	printf (" par[0:7]=%lx", ((reg & 0xff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SRAM_TEST_MODE", filter) != NULL))
	{
	reg = regs->sramTestMode; reg = LONG_ACC(reg);
	printf("%sRegister: SRAM_TEST_MODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sramTestMode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SRAM_ERR_ADRS_HI", filter) != NULL))
	{
	reg = regs->sramErrAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: SRAM_ERR_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sramErrAdrsHi, reg);
	printf ("\n%s         ", p);
	printf (" addr[0:3]=%lx", ((reg & 0xf)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVBNK0_PARMS", filter) != NULL))
	{
	reg = regs->devbnk0Parms; reg = LONG_ACC(reg);
	printf("%sRegister: DEVBNK0_PARMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->devbnk0Parms, reg);
	printf ("\n%s         ", p);
	printf (" turnoff[0:2]=%lx", ((reg & 0x7)>>0));
	printf (" acc2first[3:6]=%lx", ((reg & 0x78)>>3));
	printf (" acc2next[7:10]=%lx", ((reg & 0x780)>>7));
	printf (" ale2wr[11:13]=%lx", ((reg & 0x3800)>>11));
	printf ("\n%s         ", p);
	printf (" wrlow[14:16]=%lx", ((reg & 0x1c000)>>14));
	printf (" wrhigh[17:19]=%lx", ((reg & 0xe0000)>>17));
	printf (" dev_wdth[20:21]=%lx", ((reg & 0x300000)>>20));
	printf (" turnoffx[22]=%lx", ((reg & 0x400000)>>22));
	printf ("\n%s         ", p);
	printf (" acc2firstx[23]=%lx", ((reg & 0x800000)>>23));
	printf (" acc2nextx[24]=%lx", ((reg & 0x1000000)>>24));
	printf (" ale2wrx[25]=%lx", ((reg & 0x2000000)>>25));
	printf (" wrlowx[26]=%lx", ((reg & 0x4000000)>>26));
	printf ("\n%s         ", p);
	printf (" wrhighx[27]=%lx", ((reg & 0x8000000)>>27));
	printf (" badrskew[28:29]=%lx", ((reg & 0x30000000)>>28));
	printf (" dpen[30]=%lx", ((reg & 0x40000000)>>30));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVBNK1_PARMS", filter) != NULL))
	{
	reg = regs->devbnk1Parms; reg = LONG_ACC(reg);
	printf("%sRegister: DEVBNK1_PARMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->devbnk1Parms, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVBNK2_PARMS", filter) != NULL))
	{
	reg = regs->devbnk2Parms; reg = LONG_ACC(reg);
	printf("%sRegister: DEVBNK2_PARMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->devbnk2Parms, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVBNK3_PARMS", filter) != NULL))
	{
	reg = regs->devbnk3Parms; reg = LONG_ACC(reg);
	printf("%sRegister: DEVBNK3_PARMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->devbnk3Parms, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_BOOT_BANK_PARMS", filter) != NULL))
	{
	reg = regs->deviceBootBankParms; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_BOOT_BANK_PARMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceBootBankParms, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_CTRL", filter) != NULL))
	{
	reg = regs->deviceCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_CROSS_BAR_TIMEOUT", filter) != NULL))
	{
	reg = regs->deviceCrossBarTimeout; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_CROSS_BAR_TIMEOUT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceCrossBarTimeout, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_CROSS_BAR_CTRL_LOW", filter) != NULL))
	{
	reg = regs->deviceCrossBarCtrlLow; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_CROSS_BAR_CTRL_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceCrossBarCtrlLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_CROSS_BAR_CTRL_HI", filter) != NULL))
	{
	reg = regs->deviceCrossBarCtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_CROSS_BAR_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceCrossBarCtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_IRQ_CAUSE", filter) != NULL))
	{
	reg = regs->deviceIrqCause; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_IRQ_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceIrqCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_IRQ_MASK", filter) != NULL))
	{
	reg = regs->deviceIrqMask; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_IRQ_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceIrqMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_ERROR_ADRS", filter) != NULL))
	{
	reg = regs->deviceErrorAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_ERROR_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceErrorAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_ERR_DATA", filter) != NULL))
	{
	reg = regs->deviceErrData; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_ERR_DATA  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceErrData, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DEVICE_ERR_PARITY", filter) != NULL))
	{
	reg = regs->deviceErrParity; reg = LONG_ACC(reg);
	printf("%sRegister: DEVICE_ERR_PARITY  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->deviceErrParity, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_DMA_BYTE_COUNT", filter) != NULL))
	{
	reg = regs->chan0DmaByteCount; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_DMA_BYTE_COUNT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0DmaByteCount, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_DMA_BYTE_COUNT", filter) != NULL))
	{
	reg = regs->chan1DmaByteCount; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_DMA_BYTE_COUNT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1DmaByteCount, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN2_DMA_BYTE_COUNT", filter) != NULL))
	{
	reg = regs->chan2DmaByteCount; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN2_DMA_BYTE_COUNT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan2DmaByteCount, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN3_DMA_BYTE_COUNT", filter) != NULL))
	{
	reg = regs->chan3DmaByteCount; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN3_DMA_BYTE_COUNT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan3DmaByteCount, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_DMA_SRC_ADRS", filter) != NULL))
	{
	reg = regs->chan0DmaSrcAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_DMA_SRC_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0DmaSrcAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_DMA_SRC_ADRS", filter) != NULL))
	{
	reg = regs->chan1DmaSrcAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_DMA_SRC_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1DmaSrcAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN2_DMA_SRC_ADRS", filter) != NULL))
	{
	reg = regs->chan2DmaSrcAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN2_DMA_SRC_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan2DmaSrcAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN3_DMA_SRC_ADRS", filter) != NULL))
	{
	reg = regs->chan3DmaSrcAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN3_DMA_SRC_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan3DmaSrcAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_DMA_DST_ADRS", filter) != NULL))
	{
	reg = regs->chan0DmaDstAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_DMA_DST_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0DmaDstAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_DMA_DST_ADRS", filter) != NULL))
	{
	reg = regs->chan1DmaDstAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_DMA_DST_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1DmaDstAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN2_DMA_DST_ADRS", filter) != NULL))
	{
	reg = regs->chan2DmaDstAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN2_DMA_DST_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan2DmaDstAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN3_DMA_DST_ADRS", filter) != NULL))
	{
	reg = regs->chan3DmaDstAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN3_DMA_DST_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan3DmaDstAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0NEXT_REC_PTR", filter) != NULL))
	{
	reg = regs->chan0nextRecPtr; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0NEXT_REC_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0nextRecPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1NEXT_REC_PTR", filter) != NULL))
	{
	reg = regs->chan1nextRecPtr; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1NEXT_REC_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1nextRecPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN2NEXT_REC_PTR", filter) != NULL))
	{
	reg = regs->chan2nextRecPtr; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN2NEXT_REC_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan2nextRecPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN3NEXT_REC_PTR", filter) != NULL))
	{
	reg = regs->chan3nextRecPtr; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN3NEXT_REC_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan3nextRecPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_CTRL", filter) != NULL))
	{
	reg = regs->chan0Ctrl; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0Ctrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_CTRL", filter) != NULL))
	{
	reg = regs->chan1Ctrl; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1Ctrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN2_CTRL", filter) != NULL))
	{
	reg = regs->chan2Ctrl; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN2_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan2Ctrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN3_CTRL", filter) != NULL))
	{
	reg = regs->chan3Ctrl; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN3_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan3Ctrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TIMER_COUNTER0", filter) != NULL))
	{
	reg = regs->timerCounter0; reg = LONG_ACC(reg);
	printf("%sRegister: TIMER_COUNTER0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->timerCounter0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TIMER_COUNTER1", filter) != NULL))
	{
	reg = regs->timerCounter1; reg = LONG_ACC(reg);
	printf("%sRegister: TIMER_COUNTER1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->timerCounter1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TIMER_COUNTER2", filter) != NULL))
	{
	reg = regs->timerCounter2; reg = LONG_ACC(reg);
	printf("%sRegister: TIMER_COUNTER2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->timerCounter2, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TIMER_COUNTER3", filter) != NULL))
	{
	reg = regs->timerCounter3; reg = LONG_ACC(reg);
	printf("%sRegister: TIMER_COUNTER3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->timerCounter3, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ARBITER_CTRL_0_3", filter) != NULL))
	{
	reg = regs->arbiterCtrl03; reg = LONG_ACC(reg);
	printf("%sRegister: ARBITER_CTRL_0_3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->arbiterCtrl03, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TIMER_COUNTER_0_3_CTRL", filter) != NULL))
	{
	reg = regs->timerCounter03Ctrl; reg = LONG_ACC(reg);
	printf("%sRegister: TIMER_COUNTER_0_3_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->timerCounter03Ctrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TIMER_COUNTER_0_3_IRQ_CAUSE", filter) != NULL))
	{
	reg = regs->timerCounter03IrqCause; reg = LONG_ACC(reg);
	printf("%sRegister: TIMER_COUNTER_0_3_IRQ_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->timerCounter03IrqCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TIMER_COUNTER_0_3_IRQ_MASK", filter) != NULL))
	{
	reg = regs->timerCounter03IrqMask; reg = LONG_ACC(reg);
	printf("%sRegister: TIMER_COUNTER_0_3_IRQ_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->timerCounter03IrqMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_CUR_DESCR_PTR", filter) != NULL))
	{
	reg = regs->chan0CurDescrPtr; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_CUR_DESCR_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0CurDescrPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_CUR_DESCR_PTR", filter) != NULL))
	{
	reg = regs->chan1CurDescrPtr; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_CUR_DESCR_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1CurDescrPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN2_CUR_DESCR_PTR", filter) != NULL))
	{
	reg = regs->chan2CurDescrPtr; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN2_CUR_DESCR_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan2CurDescrPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN3_CUR_DESCR_PTR", filter) != NULL))
	{
	reg = regs->chan3CurDescrPtr; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN3_CUR_DESCR_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan3CurDescrPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_CTRL_HI", filter) != NULL))
	{
	reg = regs->chan0CtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0CtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_CTRL_HI", filter) != NULL))
	{
	reg = regs->chan1CtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1CtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN2_CTRL_HI", filter) != NULL))
	{
	reg = regs->chan2CtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN2_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan2CtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN3_CTRL_HI", filter) != NULL))
	{
	reg = regs->chan3CtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN3_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan3CtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_DMA_SRC_HI_PCI_ADRS", filter) != NULL))
	{
	reg = regs->chan0DmaSrcHiPciAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_DMA_SRC_HI_PCI_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0DmaSrcHiPciAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_DMA_SRC_HI_PCI_ADRS", filter) != NULL))
	{
	reg = regs->chan1DmaSrcHiPciAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_DMA_SRC_HI_PCI_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1DmaSrcHiPciAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN2_DMA_SRC_HI_PCI_ADRS", filter) != NULL))
	{
	reg = regs->chan2DmaSrcHiPciAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN2_DMA_SRC_HI_PCI_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan2DmaSrcHiPciAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN3_DMA_SRC_HI_PCI_ADRS", filter) != NULL))
	{
	reg = regs->chan3DmaSrcHiPciAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN3_DMA_SRC_HI_PCI_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan3DmaSrcHiPciAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHANELS0_3_IRQ_CAUSE", filter) != NULL))
	{
	reg = regs->chanels03IrqCause; reg = LONG_ACC(reg);
	printf("%sRegister: CHANELS0_3_IRQ_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chanels03IrqCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHANELS0_3_IRQ_MASK", filter) != NULL))
	{
	reg = regs->chanels03IrqMask; reg = LONG_ACC(reg);
	printf("%sRegister: CHANELS0_3_IRQ_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chanels03IrqMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHANELS0_3_ERROR_ADRS", filter) != NULL))
	{
	reg = regs->chanels03ErrorAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: CHANELS0_3_ERROR_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chanels03ErrorAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHANELS0_3_ERROR_SELECT", filter) != NULL))
	{
	reg = regs->chanels03ErrorSelect; reg = LONG_ACC(reg);
	printf("%sRegister: CHANELS0_3_ERROR_SELECT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chanels03ErrorSelect, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_0_DECODE", filter) != NULL))
	{
	reg = regs->dmaBaseAddr0Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_0_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddr0Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_SIZE_REG_0", filter) != NULL))
	{
	reg = regs->dmaSizeReg0; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_SIZE_REG_0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaSizeReg0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_1_DECODE", filter) != NULL))
	{
	reg = regs->dmaBaseAddr1Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_1_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddr1Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_SIZE_REG_1", filter) != NULL))
	{
	reg = regs->dmaSizeReg1; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_SIZE_REG_1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaSizeReg1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_2_DECODE", filter) != NULL))
	{
	reg = regs->dmaBaseAddr2Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_2_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddr2Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_SIZE_REG_2", filter) != NULL))
	{
	reg = regs->dmaSizeReg2; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_SIZE_REG_2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaSizeReg2, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_3_DECODE", filter) != NULL))
	{
	reg = regs->dmaBaseAddr3Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_3_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddr3Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_SIZE_REG_3", filter) != NULL))
	{
	reg = regs->dmaSizeReg3; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_SIZE_REG_3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaSizeReg3, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_4_DECODE", filter) != NULL))
	{
	reg = regs->dmaBaseAddr4Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_4_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddr4Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_SIZE_REG_4", filter) != NULL))
	{
	reg = regs->dmaSizeReg4; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_SIZE_REG_4  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaSizeReg4, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_5_DECODE", filter) != NULL))
	{
	reg = regs->dmaBaseAddr5Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_5_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddr5Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_SIZE_REG_5", filter) != NULL))
	{
	reg = regs->dmaSizeReg5; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_SIZE_REG_5  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaSizeReg5, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_6_DECODE", filter) != NULL))
	{
	reg = regs->dmaBaseAddr6Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_6_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddr6Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_SIZE_REG_6", filter) != NULL))
	{
	reg = regs->dmaSizeReg6; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_SIZE_REG_6  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaSizeReg6, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_7_DECODE", filter) != NULL))
	{
	reg = regs->dmaBaseAddr7Decode; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_7_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddr7Decode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_SIZE_REG_7", filter) != NULL))
	{
	reg = regs->dmaSizeReg7; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_SIZE_REG_7  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaSizeReg7, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_HI_ADDR_0_REMAP", filter) != NULL))
	{
	reg = regs->dmaHiAddr0Remap; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_HI_ADDR_0_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaHiAddr0Remap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_HI_ADDR_1_REMAP", filter) != NULL))
	{
	reg = regs->dmaHiAddr1Remap; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_HI_ADDR_1_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaHiAddr1Remap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_HI_ADDR_2_REMAP", filter) != NULL))
	{
	reg = regs->dmaHiAddr2Remap; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_HI_ADDR_2_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaHiAddr2Remap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_HI_ADDR_3_REMAP", filter) != NULL))
	{
	reg = regs->dmaHiAddr3Remap; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_HI_ADDR_3_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaHiAddr3Remap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_ACC_PROTECT_CH0", filter) != NULL))
	{
	reg = regs->dmaAccProtectCh0; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_ACC_PROTECT_CH0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaAccProtectCh0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_ACC_PROTECT_CH1", filter) != NULL))
	{
	reg = regs->dmaAccProtectCh1; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_ACC_PROTECT_CH1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaAccProtectCh1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_ACC_PROTECT_CH2", filter) != NULL))
	{
	reg = regs->dmaAccProtectCh2; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_ACC_PROTECT_CH2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaAccProtectCh2, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_ACC_PROTECT_CH3", filter) != NULL))
	{
	reg = regs->dmaAccProtectCh3; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_ACC_PROTECT_CH3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaAccProtectCh3, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_BASE_ADDR_EN", filter) != NULL))
	{
	reg = regs->dmaBaseAddrEn; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_BASE_ADDR_EN  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaBaseAddrEn, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_HEADER_RETRGT_CTRL", filter) != NULL))
	{
	reg = regs->dmaHeaderRetrgtCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_HEADER_RETRGT_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaHeaderRetrgtCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DMA_HEADER_RETRGT_BASE", filter) != NULL))
	{
	reg = regs->dmaHeaderRetrgtBase; reg = LONG_ACC(reg);
	printf("%sRegister: DMA_HEADER_RETRGT_BASE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dmaHeaderRetrgtBase, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_COMMAND", filter) != NULL))
	{
	reg = regs->pci0Command; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_COMMAND  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Command, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_TIMEOUT_RETRY", filter) != NULL))
	{
	reg = regs->pci0TimeoutRetry; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_TIMEOUT_RETRY  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0TimeoutRetry, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CS0_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0Cs0BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CS0_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Cs0BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CS2_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0Cs2BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CS2_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Cs2BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_DEVCS0_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0Devcs0BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DEVCS0_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Devcs0BankSize, reg);
	printf ("\n");

	}

   /* if ((filter == NULL) || (strstr ("PCI0_DEVCS3_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0Devcs3BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DEVCS3_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Devcs3BankSize, reg);
	printf ("\n");

	}

   if ((filter == NULL) || (strstr ("PCI0_SERR_MASK", filter) != NULL))
	{
	reg = regs->pci0SerrMask; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_SERR_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0SerrMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_IRQ_ACKNOWLEDGE_VIRTUAL", filter) != NULL))
	{
	reg = regs->pci0IrqAcknowledgeVirtual; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_IRQ_ACKNOWLEDGE_VIRTUAL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0IrqAcknowledgeVirtual, reg);
	printf ("\n");

	}
*/
    if ((filter == NULL) || (strstr ("MSI0_TRIGGER_TIMER", filter) != NULL))
	{
	reg = regs->msi0TriggerTimer; reg = LONG_ACC(reg);
	printf("%sRegister: MSI0_TRIGGER_TIMER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->msi0TriggerTimer, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_BASE_ADRS_REGS_ENABLE", filter) != NULL))
	{
	reg = regs->pci0BaseAdrsRegsEnable; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_BASE_ADRS_REGS_ENABLE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0BaseAdrsRegsEnable, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CS0_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0Cs0BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CS0_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Cs0BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CS2_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0Cs2BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CS2_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Cs2BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_DEVCS0_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0Devcs0BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DEVCS0_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Devcs0BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_DEVCS3_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0Devcs3BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DEVCS3_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Devcs3BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CFG_ADRS", filter) != NULL))
	{
	reg = regs->pci1CfgAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CFG_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CfgAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CFG_DATA_VIRTUAL", filter) != NULL))
	{
	reg = regs->pci1CfgDataVirtual; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CFG_DATA_VIRTUAL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CfgDataVirtual, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_COMMAND", filter) != NULL))
	{
	reg = regs->pci1Command; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_COMMAND  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Command, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_TIMEOUT_RETRY", filter) != NULL))
	{
	reg = regs->pci1TimeoutRetry; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_TIMEOUT_RETRY  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1TimeoutRetry, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CS0_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1Cs0BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CS0_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Cs0BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CS2_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1Cs2BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CS2_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Cs2BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DEVCS0_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1Devcs0BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DEVCS0_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Devcs0BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DEVCS3_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1Devcs3BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DEVCS3_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Devcs3BankSize, reg);
	printf ("\n");

	}

   /* if ((filter == NULL) || (strstr ("PCI1_SERR_MASK", filter) != NULL))
	{
	reg = regs->pci1SerrMask; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_SERR_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1SerrMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_IRQ_ACKNOWLEDGE_VIRTUAL", filter) != NULL))
	{
	reg = regs->pci1IrqAcknowledgeVirtual; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_IRQ_ACKNOWLEDGE_VIRTUAL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1IrqAcknowledgeVirtual, reg);
	printf ("\n");

	}
*/
    if ((filter == NULL) || (strstr ("MSI1_TRIGGER_TIMER", filter) != NULL))
	{
	reg = regs->msi1TriggerTimer; reg = LONG_ACC(reg);
	printf("%sRegister: MSI1_TRIGGER_TIMER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->msi1TriggerTimer, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_BASE_ADRS_REGS_ENABLE", filter) != NULL))
	{
	reg = regs->pci1BaseAdrsRegsEnable; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_BASE_ADRS_REGS_ENABLE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1BaseAdrsRegsEnable, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CS0_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1Cs0BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CS0_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Cs0BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CS2_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1Cs2BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CS2_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Cs2BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DEVCS0_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1Devcs0BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DEVCS0_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Devcs0BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DEVCS3_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1Devcs3BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DEVCS3_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Devcs3BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CFG_ADRS", filter) != NULL))
	{
	reg = regs->pci0CfgAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CFG_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CfgAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CFG_DATA_VIRTUAL", filter) != NULL))
	{
	reg = regs->pci0CfgDataVirtual; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CFG_DATA_VIRTUAL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CfgDataVirtual, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MODE", filter) != NULL))
	{
	reg = regs->pci0Mode; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_READ_BUF_DISCARD_TIMER", filter) != NULL))
	{
	reg = regs->pci0ReadBufDiscardTimer; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_READ_BUF_DISCARD_TIMER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ReadBufDiscardTimer, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CS1_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0Cs1BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CS1_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Cs1BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CS3_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0Cs3BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CS3_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Cs3BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_DEVCS1_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0Devcs1BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DEVCS1_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Devcs1BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CSBOOT_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0CsbootBankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CSBOOT_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CsbootBankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_DEVCS2_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci0Devcs2BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DEVCS2_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Devcs2BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_MEM0_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci0P2pMem0BarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_MEM0_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pMem0BarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_MEM1_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci0P2pMem1BarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_MEM1_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pMem1BarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_IO_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci0P2pIoBarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_IO_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pIoBarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CPU_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci0CpuBarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CPU_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CpuBarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_EXPANSION_ROM_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci0ExpansionRomBarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_EXPANSION_ROM_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ExpansionRomBarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ADRS_DECODE_CTRL", filter) != NULL))
	{
	reg = regs->pci0AdrsDecodeCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ADRS_DECODE_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AdrsDecodeCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CS1_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0Cs1BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CS1_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Cs1BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CS3_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0Cs3BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CS3_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Cs3BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_DEVCS1_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0Devcs1BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DEVCS1_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Devcs1BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CSBOOTCS_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0CsbootcsBaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CSBOOTCS_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CsbootcsBaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_DEVCS2_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0Devcs2BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DEVCS2_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Devcs2BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_MEM0_BASE_ADRS_REMAP_LOW", filter) != NULL))
	{
	reg = regs->pci0P2pMem0BaseAdrsRemapLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_MEM0_BASE_ADRS_REMAP_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pMem0BaseAdrsRemapLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_MEM0_BASE_ADRS_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci0P2pMem0BaseAdrsRemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_MEM0_BASE_ADRS_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pMem0BaseAdrsRemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_MEM1_BASE_ADRS_REMAP_LOW", filter) != NULL))
	{
	reg = regs->pci0P2pMem1BaseAdrsRemapLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_MEM1_BASE_ADRS_REMAP_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pMem1BaseAdrsRemapLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_MEM1_BASE_ADRS_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci0P2pMem1BaseAdrsRemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_MEM1_BASE_ADRS_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pMem1BaseAdrsRemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_IO_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0P2pIoBaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_IO_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pIoBaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CPU_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0CpuBaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CPU_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CpuBaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MODE", filter) != NULL))
	{
	reg = regs->pci1Mode; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mode, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_READ_BUF_DISCARD_TIMER", filter) != NULL))
	{
	reg = regs->pci1ReadBufDiscardTimer; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_READ_BUF_DISCARD_TIMER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ReadBufDiscardTimer, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CS1_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1Cs1BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CS1_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Cs1BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CS3_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1Cs3BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CS3_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Cs3BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DEVCS1_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1Devcs1BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DEVCS1_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Devcs1BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CSBOOT_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1CsbootBankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CSBOOT_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CsbootBankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DEVCS2_BANK_SIZE", filter) != NULL))
	{
	reg = regs->pci1Devcs2BankSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DEVCS2_BANK_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Devcs2BankSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_MEM0_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci1P2pMem0BarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_MEM0_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pMem0BarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_MEM1_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci1P2pMem1BarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_MEM1_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pMem1BarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_IO_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci1P2pIoBarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_IO_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pIoBarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CPU_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci1CpuBarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CPU_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CpuBarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_EXPANSION_ROM_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci1ExpansionRomBarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_EXPANSION_ROM_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ExpansionRomBarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ADRS_DECODE_CTRL", filter) != NULL))
	{
	reg = regs->pci1AdrsDecodeCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ADRS_DECODE_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AdrsDecodeCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CS1_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1Cs1BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CS1_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Cs1BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CS3_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1Cs3BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CS3_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Cs3BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DEVCS1_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1Devcs1BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DEVCS1_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Devcs1BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CSBOOTCS_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1CsbootcsBaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CSBOOTCS_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CsbootcsBaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DEVCS2_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1Devcs2BaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DEVCS2_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Devcs2BaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_MEM0_BASE_ADRS_REMAP_LOW", filter) != NULL))
	{
	reg = regs->pci1P2pMem0BaseAdrsRemapLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_MEM0_BASE_ADRS_REMAP_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pMem0BaseAdrsRemapLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_MEM0_BASE_ADRS_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci1P2pMem0BaseAdrsRemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_MEM0_BASE_ADRS_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pMem0BaseAdrsRemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_MEM1_BASE_ADRS_REMAP_LOW", filter) != NULL))
	{
	reg = regs->pci1P2pMem1BaseAdrsRemapLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_MEM1_BASE_ADRS_REMAP_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pMem1BaseAdrsRemapLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_MEM1_BASE_ADRS_REMAP_HI", filter) != NULL))
	{
	reg = regs->pci1P2pMem1BaseAdrsRemapHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_MEM1_BASE_ADRS_REMAP_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pMem1BaseAdrsRemapHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_IO_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1P2pIoBaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_IO_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pIoBaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CPU_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1CpuBaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CPU_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CpuBaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_SRAM_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci0SramBarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_SRAM_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0SramBarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_SRAM_BAR_SIZE", filter) != NULL))
	{
	reg = regs->pci1SramBarSize; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_SRAM_BAR_SIZE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1SramBarSize, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_EXPANSION_ROM_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci0ExpansionRomBaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_EXPANSION_ROM_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ExpansionRomBaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_HEADER_RETRGT_CTRL", filter) != NULL))
	{
	reg = regs->pci0HeaderRetrgtCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_HEADER_RETRGT_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0HeaderRetrgtCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_HEADER_RETRGT_BASE", filter) != NULL))
	{
	reg = regs->pci0HeaderRetrgtBase; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_HEADER_RETRGT_BASE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0HeaderRetrgtBase, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_HEADER_RETRGT_BASE_HI", filter) != NULL))
	{
	reg = regs->pci0HeaderRetrgtBaseHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_HEADER_RETRGT_BASE_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0HeaderRetrgtBaseHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_EXPANSION_ROM_BASE_ADRS_REMAP", filter) != NULL))
	{
	reg = regs->pci1ExpansionRomBaseAdrsRemap; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_EXPANSION_ROM_BASE_ADRS_REMAP  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ExpansionRomBaseAdrsRemap, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_HEADER_RETRGT_CTRL", filter) != NULL))
	{
	reg = regs->pci1HeaderRetrgtCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_HEADER_RETRGT_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1HeaderRetrgtCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_HEADER_RETRGT_BASE", filter) != NULL))
	{
	reg = regs->pci1HeaderRetrgtBase; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_HEADER_RETRGT_BASE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1HeaderRetrgtBase, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_HEADER_RETRGT_BASE_HI", filter) != NULL))
	{
	reg = regs->pci1HeaderRetrgtBaseHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_HEADER_RETRGT_BASE_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1HeaderRetrgtBaseHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_CFG", filter) != NULL))
	{
		 char *bnames[1][2]=
		{{"set", "1"},
		 };
	reg = regs->sdramCfg; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramCfg, reg);
	printf ("\n%s         ", p);
	printf (" refint_cnt[0:13]=%lx", ((reg & 0x3fff)>>0));
	printf (" pinter_en[14]=%lx", ((reg & 0x4000)>>14));
	printf (" vinter_en[15]=%lx", ((reg & 0x8000)>>15));
	printf (" reg_dram[17]=%lx", ((reg & 0x20000)>>17));
	printf ("\n%s         ", p);
	printf (" ecc[18]=%lx", ((reg & 0x40000)>>18));
	printf (" resv[19]=%s", bnames[0][((reg & 0x80000)>>19)]);
	printf (" dqs[20:21]=%lx", ((reg & 0x300000)>>20));
	printf (" rd_buff[26:31]=%lx", ((reg & 0xfc000000)>>26));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DUNIT_CTRL_LOW", filter) != NULL))
	{
	reg = regs->dunitCtrlLow; reg = LONG_ACC(reg);
	printf("%sRegister: DUNIT_CTRL_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dunitCtrlLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_TIMING_CTRL_LOW", filter) != NULL))
	{
	reg = regs->sdramTimingCtrlLow; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_TIMING_CTRL_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramTimingCtrlLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_TIMING_CTRL_HI", filter) != NULL))
	{
	reg = regs->sdramTimingCtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_TIMING_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramTimingCtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_ADRS_DECODE", filter) != NULL))
	{
	reg = regs->sdramAdrsDecode; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_ADRS_DECODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramAdrsDecode, reg);
	printf ("\n%s         ", p);
	printf (" addr_sel[0:3]=%lx", ((reg & 0xf)>>0));
	printf (" dcfg[4:5]=%lx", ((reg & 0x30)>>4));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_OPEN_PG_CTRL", filter) != NULL))
	{
	reg = regs->sdramOpenPgCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_OPEN_PG_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramOpenPgCtrl, reg);
	printf ("\n%s         ", p);
	printf (" op_en[0:15]=%lx", ((reg & 0xffff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_OPERATION_MODE", filter) != NULL))
	{
	reg = regs->sdramOperationMode; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_OPERATION_MODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramOperationMode, reg);
	printf ("\n%s         ", p);
	printf (" cmd[0:2]=%lx", ((reg & 0x7)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_EXT_MODE", filter) != NULL))
	{
	reg = regs->sdramExtMode; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_EXT_MODE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramExtMode, reg);
	printf ("\n%s         ", p);
	printf (" dll[0]=%lx", ((reg & 0x1)>>0));
	printf (" ds[1]=%lx", ((reg & 0x2)>>1));
	printf (" qfc[2]=%lx", ((reg & 0x4)>>2));
	printf (" om[3:13]=%lx", ((reg & 0x3ff8)>>3));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("DUNIT_CTRL_HI", filter) != NULL))
	{
	reg = regs->dunitCtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: DUNIT_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->dunitCtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_CROSS_BAR_CTRL_LOW", filter) != NULL))
	{
	reg = regs->sdramCrossBarCtrlLow; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_CROSS_BAR_CTRL_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramCrossBarCtrlLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_CROSS_BAR_CTRL_HI", filter) != NULL))
	{
	reg = regs->sdramCrossBarCtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_CROSS_BAR_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramCrossBarCtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_CROSS_BAR_TIMEOUT", filter) != NULL))
	{
	reg = regs->sdramCrossBarTimeout; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_CROSS_BAR_TIMEOUT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramCrossBarTimeout, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_ERROR_DATA_HI", filter) != NULL))
	{
	reg = regs->sdramErrorDataHi; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_ERROR_DATA_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramErrorDataHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_ERROR_DATA_LOW", filter) != NULL))
	{
	reg = regs->sdramErrorDataLow; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_ERROR_DATA_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramErrorDataLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_RECEIVED_ECC", filter) != NULL))
	{
	reg = regs->sdramReceivedEcc; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_RECEIVED_ECC  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramReceivedEcc, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_CALCULATED_ECC", filter) != NULL))
	{
	reg = regs->sdramCalculatedEcc; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_CALCULATED_ECC  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramCalculatedEcc, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_AND_DEVICE_ERROR_ADRS", filter) != NULL))
	{
	reg = regs->sdramAndDeviceErrorAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_AND_DEVICE_ERROR_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramAndDeviceErrorAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_ECC_CTRL", filter) != NULL))
	{
	reg = regs->sdramEccCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_ECC_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramEccCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDRAM_ECC_ERROR_COUNTER", filter) != NULL))
	{
	reg = regs->sdramEccErrorCounter; reg = LONG_ACC(reg);
	printf("%sRegister: SDRAM_ECC_ERROR_COUNTER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdramEccErrorCounter, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_MSG_REG0_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbMsgReg0CpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_MSG_REG0_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbMsgReg0CpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_MSG_REG1_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbMsgReg1CpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_MSG_REG1_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbMsgReg1CpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_MSG_REG0_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbMsgReg0CpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_MSG_REG0_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbMsgReg0CpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_MSG_REG1_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbMsgReg1CpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_MSG_REG1_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbMsgReg1CpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_DOORBELL_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbDoorbellCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_DOORBELL_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbDoorbellCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_IRQ_CAUSE_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbIrqCauseCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_IRQ_CAUSE_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbIrqCauseCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_IRQ_MASK_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbIrqMaskCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_IRQ_MASK_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbIrqMaskCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_DOORBELL_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbDoorbellCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_DOORBELL_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbDoorbellCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_IRQ_CAUSE_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbIrqCauseCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_IRQ_CAUSE_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbIrqCauseCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_IRQ_MASK_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbIrqMaskCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_IRQ_MASK_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbIrqMaskCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_QUEUE_PORT_VIRTUAL_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbQueuePortVirtualCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_QUEUE_PORT_VIRTUAL_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbQueuePortVirtualCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_QUEUE_PORT_VIRTUAL_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbQueuePortVirtualCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_QUEUE_PORT_VIRTUAL_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbQueuePortVirtualCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("QUEUE_CTRL_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->queueCtrlCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: QUEUE_CTRL_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->queueCtrlCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("QUEUE_BASE_ADRS_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->queueBaseAdrsCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: QUEUE_BASE_ADRS_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->queueBaseAdrsCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_FREE_HEAD_PTR_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbFreeHeadPtrCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_FREE_HEAD_PTR_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbFreeHeadPtrCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_FREE_TAIL_PTR_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbFreeTailPtrCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_FREE_TAIL_PTR_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbFreeTailPtrCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_POST_HEAD_PTR_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbPostHeadPtrCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_POST_HEAD_PTR_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbPostHeadPtrCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_POST_TAIL_PTR_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->inbPostTailPtrCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: INB_POST_TAIL_PTR_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbPostTailPtrCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_FREE_HEAD_PTR_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbFreeHeadPtrCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_FREE_HEAD_PTR_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbFreeHeadPtrCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_FREE_TAIL_PTR_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbFreeTailPtrCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_FREE_TAIL_PTR_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbFreeTailPtrCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_POST_HEAD_PTR_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbPostHeadPtrCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_POST_HEAD_PTR_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbPostHeadPtrCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_POST_TAIL_PTR_CPU_SIDE_PCI0", filter) != NULL))
	{
	reg = regs->outbPostTailPtrCpuSidePci0; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_POST_TAIL_PTR_CPU_SIDE_PCI0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbPostTailPtrCpuSidePci0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_MSG_REG0_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbMsgReg0CpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_MSG_REG0_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbMsgReg0CpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_MSG_REG1_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbMsgReg1CpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_MSG_REG1_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbMsgReg1CpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_MSG_REG0_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbMsgReg0CpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_MSG_REG0_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbMsgReg0CpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_MSG_REG1_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbMsgReg1CpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_MSG_REG1_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbMsgReg1CpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_DOORBELL_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbDoorbellCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_DOORBELL_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbDoorbellCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_IRQ_CAUSE_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbIrqCauseCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_IRQ_CAUSE_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbIrqCauseCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_IRQ_MASK_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbIrqMaskCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_IRQ_MASK_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbIrqMaskCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_DOORBELL_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbDoorbellCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_DOORBELL_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbDoorbellCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_IRQ_CAUSE_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbIrqCauseCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_IRQ_CAUSE_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbIrqCauseCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_IRQ_MASK_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbIrqMaskCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_IRQ_MASK_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbIrqMaskCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_QUEUE_PORT_VIRTUAL_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbQueuePortVirtualCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_QUEUE_PORT_VIRTUAL_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbQueuePortVirtualCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_QUEUE_PORT_VIRTUAL_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbQueuePortVirtualCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_QUEUE_PORT_VIRTUAL_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbQueuePortVirtualCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("QUEUE_CTRL_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->queueCtrlCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: QUEUE_CTRL_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->queueCtrlCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("QUEUE_BASE_ADRS_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->queueBaseAdrsCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: QUEUE_BASE_ADRS_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->queueBaseAdrsCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_FREE_HEAD_PTR_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbFreeHeadPtrCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_FREE_HEAD_PTR_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbFreeHeadPtrCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_FREE_TAIL_PTR_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbFreeTailPtrCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_FREE_TAIL_PTR_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbFreeTailPtrCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_POST_HEAD_PTR_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbPostHeadPtrCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_POST_HEAD_PTR_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbPostHeadPtrCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("INB_POST_TAIL_PTR_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->inbPostTailPtrCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: INB_POST_TAIL_PTR_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->inbPostTailPtrCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_FREE_HEAD_PTR_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbFreeHeadPtrCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_FREE_HEAD_PTR_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbFreeHeadPtrCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_FREE_TAIL_PTR_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbFreeTailPtrCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_FREE_TAIL_PTR_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbFreeTailPtrCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_POST_HEAD_PTR_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbPostHeadPtrCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_POST_HEAD_PTR_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbPostHeadPtrCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("OUTB_POST_TAIL_PTR_CPU_SIDE_PCI1", filter) != NULL))
	{
	reg = regs->outbPostTailPtrCpuSidePci1; reg = LONG_ACC(reg);
	printf("%sRegister: OUTB_POST_TAIL_PTR_CPU_SIDE_PCI1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->outbPostTailPtrCpuSidePci1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ARBITER_CTRL", filter) != NULL))
	{
	reg = regs->pci0ArbiterCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ARBITER_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ArbiterCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CROSS_BAR_TIMEOUT", filter) != NULL))
	{
	reg = regs->pci0CrossBarTimeout; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CROSS_BAR_TIMEOUT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CrossBarTimeout, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CROSS_BAR_CTRL_LOW", filter) != NULL))
	{
	reg = regs->pci0CrossBarCtrlLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CROSS_BAR_CTRL_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CrossBarCtrlLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_CROSS_BAR_CTRL_HI", filter) != NULL))
	{
	reg = regs->pci0CrossBarCtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_CROSS_BAR_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0CrossBarCtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_SYNC_BARRIER_VIRTUAL", filter) != NULL))
	{
	reg = regs->pci0SyncBarrierVirtual; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_SYNC_BARRIER_VIRTUAL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0SyncBarrierVirtual, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_P2P_CFG", filter) != NULL))
	{
	reg = regs->pci0P2pCfg; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_P2P_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0P2pCfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_SYNC_BARRIER_TRIGGER", filter) != NULL))
	{
	reg = regs->pci0SyncBarrierTrigger; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_SYNC_BARRIER_TRIGGER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0SyncBarrierTrigger, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MPP_PADS_CALIBRATION", filter) != NULL))
	{
	reg = regs->pci0MppPadsCalibration; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MPP_PADS_CALIBRATION  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0MppPadsCalibration, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_DLL_STATUS_CTRL", filter) != NULL))
	{
	reg = regs->pci0DllStatusCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_DLL_STATUS_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0DllStatusCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_MMASK", filter) != NULL))
	{
	reg = regs->pci0Mmask; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_MMASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0Mmask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ERROR_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pci0ErrorAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ERROR_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ErrorAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ERROR_ADRS_HI", filter) != NULL))
	{
	reg = regs->pci0ErrorAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ERROR_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ErrorAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ERROR_ATTRIB", filter) != NULL))
	{
	reg = regs->pci0ErrorAttrib; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ERROR_ATTRIB  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ErrorAttrib, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ERROR_CAUSE", filter) != NULL))
	{
	reg = regs->pci0ErrorCause; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ERROR_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ErrorCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ERROR_MASK", filter) != NULL))
	{
	reg = regs->pci0ErrorMask; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ERROR_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0ErrorMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ARBITER_CTRL", filter) != NULL))
	{
	reg = regs->pci1ArbiterCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ARBITER_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ArbiterCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CROSS_BAR_TIMEOUT", filter) != NULL))
	{
	reg = regs->pci1CrossBarTimeout; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CROSS_BAR_TIMEOUT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CrossBarTimeout, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CROSS_BAR_CTRL_LOW", filter) != NULL))
	{
	reg = regs->pci1CrossBarCtrlLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CROSS_BAR_CTRL_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CrossBarCtrlLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_CROSS_BAR_CTRL_HI", filter) != NULL))
	{
	reg = regs->pci1CrossBarCtrlHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_CROSS_BAR_CTRL_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1CrossBarCtrlHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_SYNC_BARRIER_VIRTUAL", filter) != NULL))
	{
	reg = regs->pci1SyncBarrierVirtual; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_SYNC_BARRIER_VIRTUAL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1SyncBarrierVirtual, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_P2P_CFG", filter) != NULL))
	{
	reg = regs->pci1P2pCfg; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_P2P_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1P2pCfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_SYNC_BARRIER_TRIGGER", filter) != NULL))
	{
	reg = regs->pci1SyncBarrierTrigger; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_SYNC_BARRIER_TRIGGER  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1SyncBarrierTrigger, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MPP_PADS_CALIBRATION", filter) != NULL))
	{
	reg = regs->pci1MppPadsCalibration; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MPP_PADS_CALIBRATION  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1MppPadsCalibration, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_DLL_STATUS_CTRL", filter) != NULL))
	{
	reg = regs->pci1DllStatusCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_DLL_STATUS_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1DllStatusCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_MMASK", filter) != NULL))
	{
	reg = regs->pci1Mmask; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_MMASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1Mmask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ERROR_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pci1ErrorAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ERROR_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ErrorAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ERROR_ADRS_HI", filter) != NULL))
	{
	reg = regs->pci1ErrorAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ERROR_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ErrorAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ERROR_ATTRIB", filter) != NULL))
	{
	reg = regs->pci1ErrorAttrib; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ERROR_ATTRIB  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ErrorAttrib, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ERROR_CAUSE", filter) != NULL))
	{
	reg = regs->pci1ErrorCause; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ERROR_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ErrorCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ERROR_MASK", filter) != NULL))
	{
	reg = regs->pci1ErrorMask; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ERROR_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1ErrorMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_0_LOW", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase0Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_0_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase0Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_0_HI", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase0Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_0_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase0Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_TOP_0", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlTop0; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_TOP_0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlTop0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_1_LOW", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase1Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_1_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase1Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_1_HI", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase1Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_1_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase1Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_TOP_1", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlTop1; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_TOP_1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlTop1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_2_LOW", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase2Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_2_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase2Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_2_HI", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase2Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_2_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase2Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_TOP_2", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlTop2; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_TOP_2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlTop2, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_3_LOW", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase3Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_3_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase3Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_3_HI", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase3Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_3_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase3Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_TOP_3", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlTop3; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_TOP_3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlTop3, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_4_LOW", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase4Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_4_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase4Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_4_HI", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase4Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_4_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase4Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_TOP_4", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlTop4; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_TOP_4  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlTop4, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_5_LOW", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase5Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_5_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase5Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_BASE_5_HI", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlBase5Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_BASE_5_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlBase5Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI0_ACCESS_CTRL_TOP_5", filter) != NULL))
	{
	reg = regs->pci0AccessCtrlTop5; reg = LONG_ACC(reg);
	printf("%sRegister: PCI0_ACCESS_CTRL_TOP_5  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci0AccessCtrlTop5, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_0_LOW", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase0Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_0_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase0Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_0_HI", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase0Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_0_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase0Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_TOP_0", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlTop0; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_TOP_0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlTop0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_1_LOW", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase1Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_1_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase1Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_1_HI", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase1Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_1_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase1Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_TOP_1", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlTop1; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_TOP_1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlTop1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_2_LOW", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase2Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_2_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase2Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_2_HI", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase2Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_2_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase2Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_TOP_2", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlTop2; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_TOP_2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlTop2, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_3_LOW", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase3Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_3_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase3Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_3_HI", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase3Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_3_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase3Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_TOP_3", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlTop3; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_TOP_3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlTop3, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_4_LOW", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase4Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_4_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase4Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_4_HI", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase4Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_4_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase4Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_TOP_4", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlTop4; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_TOP_4  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlTop4, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_5_LOW", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase5Low; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_5_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase5Low, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_BASE_5_HI", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlBase5Hi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_BASE_5_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlBase5Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI1_ACCESS_CTRL_TOP_5", filter) != NULL))
	{
	reg = regs->pci1AccessCtrlTop5; reg = LONG_ACC(reg);
	printf("%sRegister: PCI1_ACCESS_CTRL_TOP_5  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pci1AccessCtrlTop5, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_PHY_ADRS", filter) != NULL))
	{
	reg = regs->ethPhyAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_PHY_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethPhyAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_SMI", filter) != NULL))
	{
	reg = regs->ethSmi; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_SMI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethSmi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_DEFAULT_ADRS", filter) != NULL))
	{
	reg = regs->ethDefaultAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_DEFAULT_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethDefaultAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_DEFAULT_ID", filter) != NULL))
	{
	reg = regs->ethDefaultId; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_DEFAULT_ID  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethDefaultId, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_INT_CAUSE", filter) != NULL))
	{
	reg = regs->ethIntCause; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_INT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethIntCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_INT_MASK", filter) != NULL))
	{
	reg = regs->ethIntMask; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_INT_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethIntMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_ERR_ADRS", filter) != NULL))
	{
	reg = regs->ethErrAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_ERR_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethErrAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_INTERNAL_ADRS_ERR", filter) != NULL))
	{
	reg = regs->ethInternalAdrsErr; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_INTERNAL_ADRS_ERR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethInternalAdrsErr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_0_BASE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow0BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_0_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow0BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_0_SIZE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow0SizeAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_0_SIZE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow0SizeAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_1_BASE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow1BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_1_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow1BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_1_SIZE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow1SizeAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_1_SIZE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow1SizeAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_2_BASE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow2BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_2_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow2BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_2_SIZE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow2SizeAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_2_SIZE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow2SizeAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_3_BASE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow3BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_3_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow3BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_3_SIZE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow3SizeAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_3_SIZE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow3SizeAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_4_BASE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow4BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_4_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow4BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_4_SIZE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow4SizeAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_4_SIZE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow4SizeAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_5_BASE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow5BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_5_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow5BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_WINDOW_5_SIZE_ADRS", filter) != NULL))
	{
	reg = regs->ethWindow5SizeAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_WINDOW_5_SIZE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethWindow5SizeAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("HEADER_RETARGET_BASE", filter) != NULL))
	{
	reg = regs->headerRetargetBase; reg = LONG_ACC(reg);
	printf("%sRegister: HEADER_RETARGET_BASE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->headerRetargetBase, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("HEADER_RETARGET_CTRL", filter) != NULL))
	{
	reg = regs->headerRetargetCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: HEADER_RETARGET_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->headerRetargetCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_ADRS_REMAP_0_HI", filter) != NULL))
	{
	reg = regs->ethAdrsRemap0Hi; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_ADRS_REMAP_0_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethAdrsRemap0Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_ADRS_REMAP_1_HI", filter) != NULL))
	{
	reg = regs->ethAdrsRemap1Hi; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_ADRS_REMAP_1_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethAdrsRemap1Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_ADRS_REMAP_2_HI", filter) != NULL))
	{
	reg = regs->ethAdrsRemap2Hi; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_ADRS_REMAP_2_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethAdrsRemap2Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_ADRS_REMAP_3_HI", filter) != NULL))
	{
	reg = regs->ethAdrsRemap3Hi; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_ADRS_REMAP_3_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethAdrsRemap3Hi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_BASE_ADRS_ENABLE", filter) != NULL))
	{
	reg = regs->ethBaseAdrsEnable; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_BASE_ADRS_ENABLE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->ethBaseAdrsEnable, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_0_ACCESS_PROTECT", filter) != NULL))
	{
	reg = regs->eth0AccessProtect; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_0_ACCESS_PROTECT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0AccessProtect, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_1_ACCESS_PROTECT", filter) != NULL))
	{
	reg = regs->eth1AccessProtect; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_1_ACCESS_PROTECT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1AccessProtect, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH_2_ACCESS_PROTECT", filter) != NULL))
	{
	reg = regs->eth2AccessProtect; reg = LONG_ACC(reg);
	printf("%sRegister: ETH_2_ACCESS_PROTECT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2AccessProtect, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_PORT_CFG", filter) != NULL))
	{
	reg = regs->eth0PortCfg; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_PORT_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0PortCfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_PORT_CFG_EXTEND", filter) != NULL))
	{
	reg = regs->eth0PortCfgExtend; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_PORT_CFG_EXTEND  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0PortCfgExtend, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_MII_SERIAL_PARAMS", filter) != NULL))
	{
	reg = regs->eth0MiiSerialParams; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_MII_SERIAL_PARAMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0MiiSerialParams, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_GMII_SERIAL_PARAMS", filter) != NULL))
	{
	reg = regs->eth0GmiiSerialParams; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_GMII_SERIAL_PARAMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0GmiiSerialParams, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_VLAN_TYPE", filter) != NULL))
	{
	reg = regs->eth0VlanType; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_VLAN_TYPE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0VlanType, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_MAC_ADDR_LOW", filter) != NULL))
	{
	reg = regs->eth0MacAddrLow; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_MAC_ADDR_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0MacAddrLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_MAC_ADDR_HI", filter) != NULL))
	{
	reg = regs->eth0MacAddrHi; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_MAC_ADDR_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0MacAddrHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_SDMA_CFG", filter) != NULL))
	{
	reg = regs->eth0SdmaCfg; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_SDMA_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0SdmaCfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_SERIAL_CTRL", filter) != NULL))
	{
	reg = regs->eth0SerialCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_SERIAL_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0SerialCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_PORT_STATUS", filter) != NULL))
	{
	reg = regs->eth0PortStatus; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_PORT_STATUS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0PortStatus, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_MAX_TX_UNIT", filter) != NULL))
	{
	reg = regs->eth0MaxTxUnit; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_MAX_TX_UNIT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0MaxTxUnit, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_PORT_INT_CAUSE", filter) != NULL))
	{
	reg = regs->eth0PortIntCause; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_PORT_INT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0PortIntCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_PORT_EXT_INT_CAUSE", filter) != NULL))
	{
	reg = regs->eth0PortExtIntCause; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_PORT_EXT_INT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0PortExtIntCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_PORT_INT_MASK", filter) != NULL))
	{
	reg = regs->eth0PortIntMask; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_PORT_INT_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0PortIntMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_PORT_EXT_INT_MASK", filter) != NULL))
	{
	reg = regs->eth0PortExtIntMask; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_PORT_EXT_INT_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0PortExtIntMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH0_PORT_INTERNAL_ERR", filter) != NULL))
	{
	reg = regs->eth0PortInternalErr; reg = LONG_ACC(reg);
	printf("%sRegister: ETH0_PORT_INTERNAL_ERR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth0PortInternalErr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_PORT_CFG", filter) != NULL))
	{
	reg = regs->eth1PortCfg; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_PORT_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1PortCfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_PORT_CFG_EXTEND", filter) != NULL))
	{
	reg = regs->eth1PortCfgExtend; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_PORT_CFG_EXTEND  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1PortCfgExtend, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_MII_SERIAL_PARAMS", filter) != NULL))
	{
	reg = regs->eth1MiiSerialParams; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_MII_SERIAL_PARAMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1MiiSerialParams, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_GMII_SERIAL_PARAMS", filter) != NULL))
	{
	reg = regs->eth1GmiiSerialParams; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_GMII_SERIAL_PARAMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1GmiiSerialParams, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_VLAN_TYPE", filter) != NULL))
	{
	reg = regs->eth1VlanType; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_VLAN_TYPE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1VlanType, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_MAC_ADDR_LOW", filter) != NULL))
	{
	reg = regs->eth1MacAddrLow; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_MAC_ADDR_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1MacAddrLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_MAC_ADDR_HI", filter) != NULL))
	{
	reg = regs->eth1MacAddrHi; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_MAC_ADDR_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1MacAddrHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_SDMA_CFG", filter) != NULL))
	{
	reg = regs->eth1SdmaCfg; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_SDMA_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1SdmaCfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_SERIAL_CTRL", filter) != NULL))
	{
	reg = regs->eth1SerialCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_SERIAL_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1SerialCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_PORT_STATUS", filter) != NULL))
	{
	reg = regs->eth1PortStatus; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_PORT_STATUS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1PortStatus, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_MAX_TX_UNIT", filter) != NULL))
	{
	reg = regs->eth1MaxTxUnit; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_MAX_TX_UNIT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1MaxTxUnit, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_PORT_INT_CAUSE", filter) != NULL))
	{
	reg = regs->eth1PortIntCause; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_PORT_INT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1PortIntCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_PORT_EXT_INT_CAUSE", filter) != NULL))
	{
	reg = regs->eth1PortExtIntCause; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_PORT_EXT_INT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1PortExtIntCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_PORT_INT_MASK", filter) != NULL))
	{
	reg = regs->eth1PortIntMask; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_PORT_INT_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1PortIntMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_PORT_EXT_INT_MASK", filter) != NULL))
	{
	reg = regs->eth1PortExtIntMask; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_PORT_EXT_INT_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1PortExtIntMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH1_PORT_INTERNAL_ERR", filter) != NULL))
	{
	reg = regs->eth1PortInternalErr; reg = LONG_ACC(reg);
	printf("%sRegister: ETH1_PORT_INTERNAL_ERR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth1PortInternalErr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_PORT_CFG", filter) != NULL))
	{
	reg = regs->eth2PortCfg; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_PORT_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2PortCfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_PORT_CFG_EXTEND", filter) != NULL))
	{
	reg = regs->eth2PortCfgExtend; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_PORT_CFG_EXTEND  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2PortCfgExtend, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_MII_SERIAL_PARAMS", filter) != NULL))
	{
	reg = regs->eth2MiiSerialParams; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_MII_SERIAL_PARAMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2MiiSerialParams, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_GMII_SERIAL_PARAMS", filter) != NULL))
	{
	reg = regs->eth2GmiiSerialParams; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_GMII_SERIAL_PARAMS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2GmiiSerialParams, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_VLAN_TYPE", filter) != NULL))
	{
	reg = regs->eth2VlanType; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_VLAN_TYPE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2VlanType, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_MAC_ADDR_LOW", filter) != NULL))
	{
	reg = regs->eth2MacAddrLow; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_MAC_ADDR_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2MacAddrLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_MAC_ADDR_HI", filter) != NULL))
	{
	reg = regs->eth2MacAddrHi; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_MAC_ADDR_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2MacAddrHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_SDMA_CFG", filter) != NULL))
	{
	reg = regs->eth2SdmaCfg; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_SDMA_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2SdmaCfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_SERIAL_CTRL", filter) != NULL))
	{
	reg = regs->eth2SerialCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_SERIAL_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2SerialCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_PORT_STATUS", filter) != NULL))
	{
	reg = regs->eth2PortStatus; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_PORT_STATUS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2PortStatus, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_MAX_TX_UNIT", filter) != NULL))
	{
	reg = regs->eth2MaxTxUnit; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_MAX_TX_UNIT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2MaxTxUnit, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_PORT_INT_CAUSE", filter) != NULL))
	{
	reg = regs->eth2PortIntCause; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_PORT_INT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2PortIntCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_PORT_EXT_INT_CAUSE", filter) != NULL))
	{
	reg = regs->eth2PortExtIntCause; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_PORT_EXT_INT_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2PortExtIntCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_PORT_INT_MASK", filter) != NULL))
	{
	reg = regs->eth2PortIntMask; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_PORT_INT_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2PortIntMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_PORT_EXT_INT_MASK", filter) != NULL))
	{
	reg = regs->eth2PortExtIntMask; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_PORT_EXT_INT_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2PortExtIntMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("ETH2_PORT_INTERNAL_ERR", filter) != NULL))
	{
	reg = regs->eth2PortInternalErr; reg = LONG_ACC(reg);
	printf("%sRegister: ETH2_PORT_INTERNAL_ERR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->eth2PortInternalErr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_CFG", filter) != NULL))
	{
	reg = regs->chan0Cfg; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0Cfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN0_COMMAND", filter) != NULL))
	{
	reg = regs->chan0Command; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN0_COMMAND  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan0Command, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_CFG", filter) != NULL))
	{
	reg = regs->chan1Cfg; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1Cfg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("CHAN1_COMMAND", filter) != NULL))
	{
	reg = regs->chan1Command; reg = LONG_ACC(reg);
	printf("%sRegister: CHAN1_COMMAND  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->chan1Command, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC0_LOW", filter) != NULL))
	{
	printf ("%sMPSC0_LOW  (Address 0x%08x)\n", p, (int)&regs->mpsc0Low);
	{
		sprintf (np, "  %s", p);
		mpsc_regs_dump( &regs->mpsc0Low, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("MPSC1_LOW", filter) != NULL))
	{
	printf ("%sMPSC1_LOW  (Address 0x%08x)\n", p, (int)&regs->mpsc1Low);
	{
		sprintf (np, "  %s", p);
		mpsc_regs_dump( &regs->mpsc1Low, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("BRG0", filter) != NULL))
	{
	printf ("%sBRG0  (Address 0x%08x)\n", p, (int)&regs->brg0);
	{
		sprintf (np, "  %s", p);
		brg_regs_dump( &regs->brg0, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("BRG1", filter) != NULL))
	{
	printf ("%sBRG1  (Address 0x%08x)\n", p, (int)&regs->brg1);
	{
		sprintf (np, "  %s", p);
		brg_regs_dump( &regs->brg1, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("MAIN_ROUTING", filter) != NULL))
	{
		 char *bnames[2][8]=
		{{"sp0", "1", "2", "3", "4", "5", "6", "uncn"},
		 {"sp1", "1", "2", "3", "4", "5", "6", "uncn"},
		 };
	reg = regs->mainRouting; reg = LONG_ACC(reg);
	printf("%sRegister: MAIN_ROUTING  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mainRouting, reg);
	printf ("\n%s         ", p);
	printf (" mr0[0:2]=%s", bnames[0][((reg & 0x7)>>0)]);
	printf (" set1[3:5]=%lx", ((reg & 0x38)>>3));
	printf (" mr1[6:8]=%s", bnames[1][((reg & 0x1c0)>>6)]);
	printf (" set2[9:31]=%lx", ((reg & 0xfffffe00)>>9));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("RX_CLOCK_ROUTING", filter) != NULL))
	{
		 char *bnames[2][16]=
		{{"BRG0", "BRG1", "2", "3", "4", "5", "6", "7", "SCLK0", "9", "a", "b", "c", "d", "e", "f"},
		 {"BRG0", "BRG1", "2", "3", "4", "5", "6", "7", "SCLK1", "9", "a", "b", "c", "d", "e", "f"},
		 };
	reg = regs->rxClockRouting; reg = LONG_ACC(reg);
	printf("%sRegister: RX_CLOCK_ROUTING  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->rxClockRouting, reg);
	printf ("\n%s         ", p);
	printf (" CRR0[0:3]=%s", bnames[0][((reg & 0xf)>>0)]);
	printf (" CRR1[8:11]=%s", bnames[1][((reg & 0xf00)>>8)]);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TX_CLOCK_ROUTING", filter) != NULL))
	{
		 char *bnames[2][16]=
		{{"BRG0", "BRG1", "2", "3", "4", "5", "6", "7", "SCLK0", "TSCLK0", "a", "b", "c", "d", "e", "f"},
		 {"BRG0", "BRG1", "2", "3", "4", "5", "6", "7", "SCLK1", "TSCLK1", "a", "b", "c", "d", "e", "f"},
		 };
	reg = regs->txClockRouting; reg = LONG_ACC(reg);
	printf("%sRegister: TX_CLOCK_ROUTING  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->txClockRouting, reg);
	printf ("\n%s         ", p);
	printf (" CRT0[0:3]=%s", bnames[0][((reg & 0xf)>>0)]);
	printf (" CRT1[8:11]=%s", bnames[1][((reg & 0xf00)>>8)]);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("COMM_UNIT_CONFIGURATION", filter) != NULL))
	{
	reg = regs->commUnitConfiguration; reg = LONG_ACC(reg);
	printf("%sRegister: COMM_UNIT_CONFIGURATION  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->commUnitConfiguration, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("WATCHDOG_CFG", filter) != NULL))
	{
	reg = regs->watchdogCfg; reg = LONG_ACC(reg);
	printf("%sRegister: WATCHDOG_CFG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->watchdogCfg, reg);
	printf ("\n%s         ", p);
	printf (" preset[0:23]=%lx", ((reg & 0xffffff)>>0));
	printf (" ctl1[24:25]=%lx", ((reg & 0x3000000)>>24));
	printf (" ctl2[26:27]=%lx", ((reg & 0xc000000)>>26));
	printf (" nmi[29]=%lx", ((reg & 0x20000000)>>29));
	printf ("\n%s         ", p);
	printf (" wde[30]=%lx", ((reg & 0x40000000)>>30));
	printf (" en[31]=%lx", ((reg & 0x80000000)>>31));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("WATCHDOG_VALUE", filter) != NULL))
	{
	reg = regs->watchdogValue; reg = LONG_ACC(reg);
	printf("%sRegister: WATCHDOG_VALUE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->watchdogValue, reg);
	printf ("\n%s         ", p);
	printf (" nmival[0:23]=%lx", ((reg & 0xffffff)>>0));
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDMA_CAUSE", filter) != NULL))
	{
	reg = regs->sdmaCause; reg = LONG_ACC(reg);
	printf("%sRegister: SDMA_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdmaCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC0_CAUSE", filter) != NULL))
	{
	printf ("%sMPSC0_CAUSE  (Address 0x%08x)\n", p, (int)&regs->mpsc0Cause);
	{
		sprintf (np, "  %s", p);
		mpsc_cause_regs_dump( &regs->mpsc0Cause, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("MPSC1_CAUSE", filter) != NULL))
	{
	printf ("%sMPSC1_CAUSE  (Address 0x%08x)\n", p, (int)&regs->mpsc1Cause);
	{
		sprintf (np, "  %s", p);
		mpsc_cause_regs_dump( &regs->mpsc1Cause, np, filter );
	}
	}

    if ((filter == NULL) || (strstr ("BRG_CAUSE", filter) != NULL))
	{
	reg = regs->brgCause; reg = LONG_ACC(reg);
	printf("%sRegister: BRG_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->brgCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SDMA_MASK", filter) != NULL))
	{
	reg = regs->sdmaMask; reg = LONG_ACC(reg);
	printf("%sRegister: SDMA_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->sdmaMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC0_MASK", filter) != NULL))
	{
	reg = regs->mpsc0Mask; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC0_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpsc0Mask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC1_MASK", filter) != NULL))
	{
	reg = regs->mpsc1Mask; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC1_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpsc1Mask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("BRG_MASK", filter) != NULL))
	{
	reg = regs->brgMask; reg = LONG_ACC(reg);
	printf("%sRegister: BRG_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->brgMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TWSI_SLAVE_ADRS", filter) != NULL))
	{
	reg = regs->twsiSlaveAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: TWSI_SLAVE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->twsiSlaveAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TWSI_DATA", filter) != NULL))
	{
	reg = regs->twsiData; reg = LONG_ACC(reg);
	printf("%sRegister: TWSI_DATA  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->twsiData, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TWSI_CTRL", filter) != NULL))
	{
	reg = regs->twsiCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: TWSI_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->twsiCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TWSI_STATUS_BAUDE_RATE", filter) != NULL))
	{
	reg = regs->twsiStatusBaudeRate; reg = LONG_ACC(reg);
	printf("%sRegister: TWSI_STATUS_BAUDE_RATE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->twsiStatusBaudeRate, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TWSI_EXTENDED_SLAVE_ADRS", filter) != NULL))
	{
	reg = regs->twsiExtendedSlaveAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: TWSI_EXTENDED_SLAVE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->twsiExtendedSlaveAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("TWSI_SOFT_RESET", filter) != NULL))
	{
	reg = regs->twsiSoftReset; reg = LONG_ACC(reg);
	printf("%sRegister: TWSI_SOFT_RESET  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->twsiSoftReset, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPP_CTRL0", filter) != NULL))
	{
	reg = regs->mppCtrl0; reg = LONG_ACC(reg);
	printf("%sRegister: MPP_CTRL0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mppCtrl0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPP_CTRL1", filter) != NULL))
	{
	reg = regs->mppCtrl1; reg = LONG_ACC(reg);
	printf("%sRegister: MPP_CTRL1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mppCtrl1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPP_CTRL2", filter) != NULL))
	{
	reg = regs->mppCtrl2; reg = LONG_ACC(reg);
	printf("%sRegister: MPP_CTRL2  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mppCtrl2, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPP_CTRL3", filter) != NULL))
	{
	reg = regs->mppCtrl3; reg = LONG_ACC(reg);
	printf("%sRegister: MPP_CTRL3  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mppCtrl3, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("GPP_IO_CTRL", filter) != NULL))
	{
	reg = regs->gppIoCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: GPP_IO_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->gppIoCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("GPP_VALUE", filter) != NULL))
	{
	reg = regs->gppValue; reg = LONG_ACC(reg);
	printf("%sRegister: GPP_VALUE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->gppValue, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("GPP_IRQ_CAUSE", filter) != NULL))
	{
	reg = regs->gppIrqCause; reg = LONG_ACC(reg);
	printf("%sRegister: GPP_IRQ_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->gppIrqCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("GPP_IRQ_MASK0", filter) != NULL))
	{
	reg = regs->gppIrqMask0; reg = LONG_ACC(reg);
	printf("%sRegister: GPP_IRQ_MASK0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->gppIrqMask0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("GPP_LEVEL_CTRL", filter) != NULL))
	{
	reg = regs->gppLevelCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: GPP_LEVEL_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->gppLevelCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("GPP_IRQ_MASK1", filter) != NULL))
	{
	reg = regs->gppIrqMask1; reg = LONG_ACC(reg);
	printf("%sRegister: GPP_IRQ_MASK1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->gppIrqMask1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_WINDOW_0_BASE_ADRS", filter) != NULL))
	{
	reg = regs->mpscWindow0BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_WINDOW_0_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscWindow0BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_WINDOW_0_SIZE_REG", filter) != NULL))
	{
	reg = regs->mpscWindow0SizeReg; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_WINDOW_0_SIZE_REG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscWindow0SizeReg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_WINDOW_1_BASE_ADRS", filter) != NULL))
	{
	reg = regs->mpscWindow1BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_WINDOW_1_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscWindow1BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_WINDOW_1_SIZE_REG", filter) != NULL))
	{
	reg = regs->mpscWindow1SizeReg; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_WINDOW_1_SIZE_REG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscWindow1SizeReg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_WINDOW_2_BASE_ADRS", filter) != NULL))
	{
	reg = regs->mpscWindow2BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_WINDOW_2_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscWindow2BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_WINDOW_2_SIZE_REG", filter) != NULL))
	{
	reg = regs->mpscWindow2SizeReg; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_WINDOW_2_SIZE_REG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscWindow2SizeReg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_WINDOW_3_BASE_ADRS", filter) != NULL))
	{
	reg = regs->mpscWindow3BaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_WINDOW_3_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscWindow3BaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_WINDOW_3_SIZE_REG", filter) != NULL))
	{
	reg = regs->mpscWindow3SizeReg; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_WINDOW_3_SIZE_REG  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscWindow3SizeReg, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_ADDR_REMAP_HI_0", filter) != NULL))
	{
	reg = regs->mpscAddrRemapHi0; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_ADDR_REMAP_HI_0  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscAddrRemapHi0, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_ADDR_REMAP_HI_1", filter) != NULL))
	{
	reg = regs->mpscAddrRemapHi1; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_ADDR_REMAP_HI_1  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscAddrRemapHi1, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_BASE_ADDR_ENABLE", filter) != NULL))
	{
	reg = regs->mpscBaseAddrEnable; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_BASE_ADDR_ENABLE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscBaseAddrEnable, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_0_ACCESS_PROTECT", filter) != NULL))
	{
	reg = regs->mpsc0AccessProtect; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_0_ACCESS_PROTECT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpsc0AccessProtect, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_1_ACCESS_PROTECT", filter) != NULL))
	{
	reg = regs->mpsc1AccessProtect; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_1_ACCESS_PROTECT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpsc1AccessProtect, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("MPSC_INTERNAL_SPACE_ADDR", filter) != NULL))
	{
	reg = regs->mpscInternalSpaceAddr; reg = LONG_ACC(reg);
	printf("%sRegister: MPSC_INTERNAL_SPACE_ADDR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->mpscInternalSpaceAddr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("COMM_UNIT_ARBITER_CTRL", filter) != NULL))
	{
	reg = regs->commUnitArbiterCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: COMM_UNIT_ARBITER_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->commUnitArbiterCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("COMM_UNIT_CROSS_BAR_TIMEOUT", filter) != NULL))
	{
	reg = regs->commUnitCrossBarTimeout; reg = LONG_ACC(reg);
	printf("%sRegister: COMM_UNIT_CROSS_BAR_TIMEOUT  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->commUnitCrossBarTimeout, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("COMM_UNIT_IRQ_CAUSE", filter) != NULL))
	{
	reg = regs->commUnitIrqCause; reg = LONG_ACC(reg);
	printf("%sRegister: COMM_UNIT_IRQ_CAUSE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->commUnitIrqCause, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("COMM_UNIT_IRQ_MASK", filter) != NULL))
	{
	reg = regs->commUnitIrqMask; reg = LONG_ACC(reg);
	printf("%sRegister: COMM_UNIT_IRQ_MASK  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->commUnitIrqMask, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("COMM_UNIT_ERROR_ADRS", filter) != NULL))
	{
	reg = regs->commUnitErrorAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: COMM_UNIT_ERROR_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->commUnitErrorAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SERIAL_INIT_LAST_DATA", filter) != NULL))
	{
	reg = regs->serialInitLastData; reg = LONG_ACC(reg);
	printf("%sRegister: SERIAL_INIT_LAST_DATA  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->serialInitLastData, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SERIAL_INIT_CTRL", filter) != NULL))
	{
	reg = regs->serialInitCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: SERIAL_INIT_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->serialInitCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("SERIAL_INIT_STATUS", filter) != NULL))
	{
	reg = regs->serialInitStatus; reg = LONG_ACC(reg);
	printf("%sRegister: SERIAL_INIT_STATUS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->serialInitStatus, reg);
	printf ("\n");

	}

    free (np);
}		/* end of MV64360_regs_dump()*/


void
pcicfg0_regs_dump( regs, p, filter)
    PCICFG0_REGS	*regs;
    char	*p;
    char	*filter;
    {
    u_long		reg;
    char *np;

    if (p == NULL) p = "";

    np=malloc (strlen (p) + 10);

    if ((filter == NULL) || (strstr ("PCI_DEVICE_AND_VENDOR_ID", filter) != NULL))
	{
	reg = regs->pciDeviceAndVendorId; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DEVICE_AND_VENDOR_ID  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDeviceAndVendorId, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_STATUS_AND_COMMAND", filter) != NULL))
	{
	reg = regs->pciStatusAndCommand; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_STATUS_AND_COMMAND  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciStatusAndCommand, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CLASS_CODE_AND_REVISION_ID", filter) != NULL))
	{
	reg = regs->pciClassCodeAndRevisionId; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CLASS_CODE_AND_REVISION_ID  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciClassCodeAndRevisionId, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_BIST_HEADER_TYPE_LATENCY_TIMER_CACHE_LINE", filter) != NULL))
	{
	reg = regs->pciBistHeaderTypeLatencyTimerCacheLine; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_BIST_HEADER_TYPE_LATENCY_TIMER_CACHE_LINE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciBistHeaderTypeLatencyTimerCacheLine, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CS0_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciCs0BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CS0_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCs0BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CS0_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciCs0BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CS0_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCs0BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CS1_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciCs1BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CS1_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCs1BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CS1_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciCs1BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CS1_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCs1BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_INTERNAL_REGS_MEM_MAPPED_BASE_ADRS", filter) != NULL))
	{
	reg = regs->pciInternalRegsMemMappedBaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_INTERNAL_REGS_MEM_MAPPED_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciInternalRegsMemMappedBaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_SUBSYSTEM_ID_AND_SUBSYSTEM_VENDOR_ID", filter) != NULL))
	{
	reg = regs->pciSubsystemIdAndSubsystemVendorId; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_SUBSYSTEM_ID_AND_SUBSYSTEM_VENDOR_ID  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciSubsystemIdAndSubsystemVendorId, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_EXPANSION_ROM_BASE_ADRS", filter) != NULL))
	{
	reg = regs->pciExpansionRomBaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_EXPANSION_ROM_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciExpansionRomBaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CAPABILTY_LIST_PTR", filter) != NULL))
	{
	reg = regs->pciCapabiltyListPtr; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CAPABILTY_LIST_PTR  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCapabiltyListPtr, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_IRQ_PIN_AND_LINE", filter) != NULL))
	{
	reg = regs->pciIrqPinAndLine; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_IRQ_PIN_AND_LINE  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciIrqPinAndLine, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_POWER_MANAGEMENT_CAPABILITY", filter) != NULL))
	{
	reg = regs->pciPowerManagementCapability; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_POWER_MANAGEMENT_CAPABILITY  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciPowerManagementCapability, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_POWER_MANAGEMENT_STATUS_AND_CTRL", filter) != NULL))
	{
	reg = regs->pciPowerManagementStatusAndCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_POWER_MANAGEMENT_STATUS_AND_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciPowerManagementStatusAndCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_VPD_ADRS", filter) != NULL))
	{
	reg = regs->pciVpdAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_VPD_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciVpdAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_VPD_DATA", filter) != NULL))
	{
	reg = regs->pciVpdData; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_VPD_DATA  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciVpdData, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_MSI_MSG_CTRL", filter) != NULL))
	{
	reg = regs->pciMsiMsgCtrl; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_MSI_MSG_CTRL  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciMsiMsgCtrl, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_MSI_MSG_ADRS", filter) != NULL))
	{
	reg = regs->pciMsiMsgAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_MSI_MSG_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciMsiMsgAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_MSI_MSG_UPPER_ADRS", filter) != NULL))
	{
	reg = regs->pciMsiMsgUpperAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_MSI_MSG_UPPER_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciMsiMsgUpperAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_MSI_MSG_DATA", filter) != NULL))
	{
	reg = regs->pciMsiMsgData; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_MSI_MSG_DATA  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciMsiMsgData, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_X_CMD", filter) != NULL))
	{
	reg = regs->pciXCmd; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_X_CMD  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciXCmd, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_X_STATUS", filter) != NULL))
	{
	reg = regs->pciXStatus; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_X_STATUS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciXStatus, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_COMPACT_PCI_HOT_SWAP_CAPABILITY", filter) != NULL))
	{
	reg = regs->pciCompactPciHotSwapCapability; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_COMPACT_PCI_HOT_SWAP_CAPABILITY  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCompactPciHotSwapCapability, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CS2_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciCs2BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CS2_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCs2BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CS2_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciCs2BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CS2_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCs2BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CS3_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciCs3BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CS3_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCs3BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CS3_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciCs3BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CS3_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCs3BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_SRAM_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciSramBaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_SRAM_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciSramBaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_SRAM_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciSramBaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_SRAM_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciSramBaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_DCS0_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciDcs0BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DCS0_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDcs0BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_DCS0_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciDcs0BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DCS0_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDcs0BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_DCS1_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciDcs1BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DCS1_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDcs1BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_DCS1_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciDcs1BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DCS1_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDcs1BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_DCS2_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciDcs2BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DCS2_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDcs2BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_DCS2_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciDcs2BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DCS2_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDcs2BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_DCS3_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciDcs3BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DCS3_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDcs3BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_DCS3_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciDcs3BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_DCS3_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciDcs3BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_BOOTCS_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciBootcsBaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_BOOTCS_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciBootcsBaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_BOOTCS_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciBootcsBaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_BOOTCS_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciBootcsBaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CPU_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciCpuBaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CPU_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCpuBaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_CPU_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciCpuBaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_CPU_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciCpuBaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_P2P_MEM0_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciP2pMem0BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_P2P_MEM0_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciP2pMem0BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_P2P_MEM0_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciP2pMem0BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_P2P_MEM0_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciP2pMem0BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_P2P_MEM1_BASE_ADRS_LOW", filter) != NULL))
	{
	reg = regs->pciP2pMem1BaseAdrsLow; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_P2P_MEM1_BASE_ADRS_LOW  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciP2pMem1BaseAdrsLow, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_P2P_MEM1_BASE_ADRS_HI", filter) != NULL))
	{
	reg = regs->pciP2pMem1BaseAdrsHi; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_P2P_MEM1_BASE_ADRS_HI  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciP2pMem1BaseAdrsHi, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_P2P_I_O_BASE_ADRS", filter) != NULL))
	{
	reg = regs->pciP2pIOBaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_P2P_I_O_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciP2pIOBaseAdrs, reg);
	printf ("\n");

	}

    if ((filter == NULL) || (strstr ("PCI_INTERNAL_REG_I_O_MAP_BASE_ADRS", filter) != NULL))
	{
	reg = regs->pciInternalRegIOMapBaseAdrs; reg = LONG_ACC(reg);
	printf("%sRegister: PCI_INTERNAL_REG_I_O_MAP_BASE_ADRS  Address: 0x%08x  Value: %08lx",
		 p, (int)&regs->pciInternalRegIOMapBaseAdrs, reg);
	printf ("\n");

	}

    free (np);
}		/* end of pcicfg0_regs_dump()*/

typedef union pci0registers
{
	unsigned int arr[48];
    PCICFG0_REGS pci0Regs;
}PCI0_REGISTERS;


int
frcTestDispAllPCI0Reg(void)
{
    PCI0_REGISTERS *regs=(PCI0_REGISTERS* )malloc(sizeof(PCI0_REGISTERS));
	int i=0;
    unsigned short offsetArr[] = {
	                               0x000,0x004,0x008,0x00C,0x010,0x014,0x018,0x01c,
							       0x020,0x02C,0x030,0x034,0x03C,0x040,0x044,0x048,
								   0x04c,0x050,0x054,0x058,0x05c,0x060,0x064,0x068,
								   0x110,0x114,0x118,0x11c,0x120,0x124,0x210,0x214,
								   0x218,0x21c,0x220,0x224,0x310,0x314,0x318,0x31c,
								   0x320,0x324,0x410,0x414,0x418,0x41c,0x420,0x424
                                  };
    for(i=0;i<48;i++)
	{
	  regs->arr[i]= frcPci0ReadConfigReg(offsetArr[i],0x00);
	}
    pcicfg0_regs_dump((PCICFG0_REGS *)regs,NULL,NULL);

	return 0;
}
