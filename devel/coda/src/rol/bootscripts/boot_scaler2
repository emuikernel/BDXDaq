#
#  Bootscript for scaler2 (VME crate on FC)
#

# adds router
cd "$CODA/VXWORKS_ppc/bootscripts"
< routeadd_mot.boot

# initializations for all rocs
cd "$CODA/VXWORKS_ppc/bootscripts"
< all.boot

# initializations for vme
cd "$CODA/VXWORKS_ppc/bootscripts"
< ppc_scaler.boot

#load scaler code
#cd "$CODA/VXWORKS_ppc/obj"
#ld < scalers.o

taskSpawn "ROC",200,0,500000,coda_roc,"-session","clasprod","-objects","scaler2 ROC","-i"

taskDelay(sysClkRateGet()*5)
taskSpawn "TCP_SERVER",250,0,100000,tcpServer







