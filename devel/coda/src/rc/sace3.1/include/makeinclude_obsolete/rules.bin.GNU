#----------------------------------------------------------------------------
#	%W%	%G%
#
#	Build binaries
#	GNU version
#	Requires GNU make
#----------------------------------------------------------------------------

VBIN    = $(BIN:%=%$(VAR))

$(BIN): %: $(VDIR)%.o $(SHOBJS)
	$(LINK.cc) -o $@ $< $(LDFLAGS) $(VLDLIBS)
