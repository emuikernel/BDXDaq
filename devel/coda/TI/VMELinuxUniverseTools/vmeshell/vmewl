#!/bin/sh
if [ $1 ]; then
  if [ `echo $1 | cut -b 1-1` = '-' ]; then
    # We do have CmdLine Parameters
    echo "wl $2 $3 $4" > tmp.vme
    vmecall tmp.vme $1
  else
    echo "wl $1 $2 $3" > tmp.vme
    vmecall tmp.vme 
  fi	       
else
  echo "Usage: "
fi  
