#!/bin/sh
if [ $1 ]; then
  if [ `echo $1 | cut -b 1-1` = '-' ]; then
    # We do have CmdLine Parameters
    echo "dl $2 $3" > tmp.vme
    vmecall tmp.vme $1
  else
    echo "dl $1 $2" > tmp.vme
    vmecall tmp.vme 
  fi	       
else
  echo "Usage: "
fi  
